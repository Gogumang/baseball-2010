# A. 이벤트 발동 조건 · 보상 · 스킬

> 판정: **확정** = 디스어셈으로 식·표까지 확인 · **유력** = 한 고리 미확인 · **미해결**
> 주소는 `binary.mod` VA (파일오프셋 + 0xfcc). 웹 파일:줄은 main `2fa9283` 기준.
> ⚠️ = 다른 세션이 자주 고치는 영역(`entities/game`·`career`·`league`, `features/play-game`, `app/model/useCareerSession.ts`) — 옮길 때 충돌 주의.

## 1. 구현 명세 — 게임에 넣을 것

### A-1. 이벤트 고르는 방식 (확정)
- **호출 시점**: 관리 화면(화면코드 105)·외출 지도(112)에 있는 동안 **매 갱신마다** 선택 함수를 부른다(0x1cdec). 통과한 이벤트가 있으면 이벤트 화면(0x72)으로 넘어갔다가 돌아온다 → 조건을 통과한 이벤트가 **하나씩 연달아** 모두 나온다.
- **선택 규칙**(0xadc70): 파일 순서로 훑어 **처음 통과한 한 건**. 우선순위·확률 없음. **커서를 이어 쓴다** — 다음 호출은 지난 당첨 위치부터, 끝까지 없으면 0 으로 되감고 그 호출은 "없음".
- 번호로 부를 때(0xae170)는 "현재 레코드 다음 위치"를 커서로 저장한다.
- **외출 장소 배정**(0x8cdc0, 지도 진입 때 한 번): 처음부터 훑어 trigger≥2 통과 이벤트마다 장소 = trigger−2, 빈 칸이면 넣는다 → 장소마다 파일 순서 첫 이벤트. 없으면 440+장소.
- **직접 호출**(0x8bdc8): 오프닝 451(새 선수, 0x8bde0), 분기(선택지·예/아니오·경기 결과), 연말 상태 132(13년차 504 / **1~6년차 380** / 7~12년차 502 — B-1 참고). *(검증 V1 정정: 노트의 "5년차 이하" 는 0-기준 idx ≤5 를 잘못 옮긴 것)*
- 웹: `src/entities/story/model/storyScene.ts` `nextEventFor` — 커서 이어 쓰기 없음. "분기 전용 63개 제외" 규칙은 원본에 없다(원본은 대상 0 으로 막는다).

### A-2. 판정 순서 (0xacfbc, 확정)
1. 대상 == 0 → 불발 (코드 호출 전용)
2. 모드별 허용 대상: 타자편(4) 1·2 · 투수편(3) 1·3 · 시즌(2) 4
3. id 490 이고 질병 쿨다운(+0x7c) > 0 → 불발
4. **본 이벤트면 불발** (반복 이벤트도. 반복 이벤트는 불러오기 때만 본 표시가 지워진다, 0xacf60)
5. trigger ↔ 화면: 0 ↔ {105, 201} · 1 ↔ 112 · ≥2 ↔ 113 (장소 인자 ≥0 이면 trigger−2 == 장소)
6. requiresEvent > 0 이면 그 이벤트를 봤어야 함
7. **날짜 창**: 바이트 a,b,c,d 중 **하나라도 0 이면 검사 생략**. `from=(a−1)·45+b`, `to=(c−1)·45+d`, `now=연차(0부터)·45+치른경기수+1`, `from ≤ now ≤ to`. 45 로 자르지 않는다.
   - 웹 `storyScene.ts` `isInDateWindow`: 연차 둘 다 0 일 때만 생략, 경기 번호를 45 로 자름 → **고칠 것**
8. 조건 목록(A-3). 조건 0개면 통과.

### A-3. 조건 종류 (switch 0xad1ba, 점프표 0xd83a0) — 확정
| 종류 | 식 | 웹 |
|---|---|---|
| 0~3 | 기본 능력치(장비·스킬 보정 전, 0xb6414 플래그 0) — 0 히트·1 파워·2 수비·3 주루. v≥0 → ≥v, v<0 → < \|v\| | 같음(음수 규칙만 없음, 데이터에도 없음) |
| 18 | 인기도(+0x48), 부호 규칙 같음 | 같음 |
| 19 | 평판(+0x62), 부호 규칙 같음 | 같음 |
| 20 | 스킬 v−1 **없을 때** 하위 조건 0xd8408[v−3] 통과 → 획득 이벤트 | **미구현 (항상 false)** |
| 21 | 스킬 v−1 **있을 때** 하위 조건 0xd8454[v−3] 통과 → 해제 이벤트 | **미구현** |
| 22 | 질병 없음 + 확률: 사기 >70 0 · >50 2 · >30 4 · >10 7 · 그 밖 14 %, **유리몸(스킬4) +10 · 행운(스킬6) −20**(바닥 0), rand[0,100) < p | 유리몸·행운 보정 **없음** → 추가 |
| 23 | 모드 2 불발, 아플 때 rand[0,100) ≤ 9 (데이터 미사용) | 불필요 |
| 24 / 25 | v≤0 이거나 이벤트 v 봤음 / 안 봤음 | 같음 |
| 4~17, 26+ | 통과 | — |

### A-4. 스킬 2~20 획득·해제 조건 (확정 — 카운터 이름 일부 유력)
표는 4절 상세 근거에 있다. 구현에 필요한 새 상태:
- `+0x70~+0x74` 연속 훈련 카운터(해당 칸 훈련이면 ++, **다른 훈련이면 전부 0**), `+0x75` 몹쓸몸 훈련 수, `+0x76` 유리몸 훈련 수
- `+0x1c0/+0x1cd` 먹튀 보유 중 인기도 변화 합·경기 수(먹튀 없으면 0)
- `+0x1c2` 이번 시즌 인기도 변화 합(경기마다 += 인기도 변화, 새 시즌 0)
- `+0x1c7` 무력감 보유 중 "경기 뒤 사기 ≥ 90" 연속 경기 수
- `+0x1d0+k` **마이너스 스킬 k 를 한 번 해제했음** → 다시 못 얻는다 (표 0xd7e10 = [2,3,4,5,17,18,19,20], 웹 `gpItems.ts:68` 과 같음)
- `+0x4b+i` 통산 훈련 칸별 횟수 / `+0x6b+i` 새 시즌 시작 때 사본 → 차이 = 이번 시즌 훈련 수
- 투수편: 스킬 비트 n(≥8)의 이름은 StrCOMMON 스킬 n+16
- 스킬 보상을 받으면 알림 팝업 (0x8d6bc)

### A-5. 스킬 16 상승세 · 17 하락세 (확정) — `swingSkills.ts:42~44`
- 타자별 링버퍼(용량 10). **읽기 i=0 이 가장 오래된 기록**.
- 상승세: 개수 > 3 이고 code(0)·code(1) 모두 1~4(안타) → 안타율·장타율 +값/20
- 하락세: 개수 > 2 이고 code(0)·code(1) 모두 5~7 → −값/10
- 웹은 `slice(-2)`(최근 둘), 개수 조건 없음 → **고칠 것**

### A-6. 보상(명령 7) 0~21 (확정) — `src/entities/story/model/eventReward.ts`
| 종류 | 동작 | 웹 |
|---|---|---|
| 0 | 인기도 +v (0~9999) | 있음 |
| 1 | 평판 +v (0~999) | 있음 |
| 2 | 사기 +v (0~100) | 있음 |
| 3 | 소지금 +v (100만 단위, 0~9999) | 있음 (웹은 만원 단위라 ×100) |
| 4 | v>0 스킬 v−1 획득 / v<0 \|v\|−1 해제. **마이너스 스킬 해제 시 +0x1d0 플래그**(다시 못 얻음) | 있음 (`applySkillReward`) — +0x1d0 플래그는 **없음** |
| 5 | 효과 없음 | — |
| 6 | 투수편만: 마구 v 습득 (+0x204+v = 1) | 없음 — 투수편과 함께 |
| 7 | 히든 오픈 \|v\| | 있음 (`openHidden`) |
| 9 | GP 아이템 v−1 효과 (0xa4488). 이벤트 36 v=10 → 아이템 9 | **없음** |
| 10 | G포인트 +v (0~99999) | 있음 |
| 11 | 값 ≥0 이면 먼저 알림 글 만들기(0x8c14e)에서 **값을 bfa55(0,4)+1 로 다시 써** 질병 종류(1~4)가 된다 → 적용(0x8c718) 때 v>0 이라 질병, 기간 3, 쿨다운 20 / 값 <0 이면 치료 | v<0 치료 · v≥0 질병 — **원본과 같음** *(P4 정정: A 노트의 "v=0 은 치료(원본 버그)" 는 값 재기록을 놓친 오판)* |
| 13~16 | 능력치 (종류−13) +v, 999 상한. 기본값(보정 전)이 타입 한계를 넘으면 한계로 | 있음 |
| 17 | **모든 능력치 +v** (13~16 처리를 4칸). 이벤트 231 v=10 | **없음** |
| 18 | 목표 타순 경로 +0x57 = v | 있음 |
| 19 | 타순 v 로 | 있음 |
| 20 | 연봉 증감 (0 +30% · 1/4 +20% · 2/5 +10% · 3 −20% · 6 +5% · 7 −10%) | 있음 (`applySalaryChange`) |
| 21 | 명령의 **첫 종류가 21 이면 엔딩 진입** (0x8d4c4) | **없음** (`default` 로 무시) |
- **연차 보정**(0x8d508): 이벤트 393·394·395 — 타자·투수편 종류 0 +3y · 1 −2y · 3 +y / 이벤트 396 — 종류 0 −4y · 1 −3y (y = 연차, 0부터). 시즌모드 값은 4절. 웹 `app/model/seasonEvents.ts` 에 **없음**.

### A-7. 화면효과(명령 5) 1~7 (식 확정) — 웹 미사용 (`eventTypes.ts:23` 정의만)
| id | 동작 |
|---|---|
| 1 | 진동 500ms (진동 설정 켜짐일 때) |
| 2 | 진동 + 화면 흔들림 |
| 3 | 화면 흔들림 6틱: (−2,0)(−1,2)(2,0)(0,−2)(1,3)(−2,−1) |
| 4 | 검게 어두워짐 (단계 0→15) |
| 5 | 하얗게 덮임 (0→15) |
| 6 | 검은 화면에서 밝아짐 (16→0) |
| 7 | 흰 섬광이 걷힘 (16→0) |
데이터 사용: fx2 56회 · fx3 28 · fx4 28 · fx6 60.

### A-8. 초상화 세로 위치 (확정, 관리 화면 값만 미해결)
- 외출 지도·장소(또는 대화 인자 ≠ 0): 바닥선 y = 화면높이 − 68 (240×320 → 252), 그 아래는 잘린다.
- 대화 상자 y = 화면높이 − 슬라이드(+15/갱신, 최대 55) + 5.

## 2. 원본 버그 — 그대로 옮기고 나중에 수정 후보
- 없음. *(처음엔 "이벤트 490 이 치료가 된다" 를 버그로 적었으나, 보상 글 만들기 0x8c14e 가 값 0 을 질병 종류 1~4 로 다시 써 두므로 실제로는 질병이 된다 — P4·G 4-3b 로 정정)*

## 3. 미해결
- 타석 기록 코드 5~9 의 정확한 뜻 (경기 객체 +0x26 값표)
- 경기 카운터 +0x1f0[1]~[6] 의 뜻 ([0] = 사이클 횟수는 확정)
- +0x1ca 연도 비트 → **B 에서 확정: 그 해 MVP 비트**
- 관리 화면 초상화 바닥 y 수치

## 4. 상세 근거 (해독 노트 전문)

