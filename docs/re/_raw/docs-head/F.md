# F. 화면 배치 (코드 안에만 있는 좌표 · 색 · 연출)

> 판정: **확정** = 디스어셈으로 식·표까지 확인 · **유력** = 한 고리 미확인 · **미해결**
> 주소는 `binary.mod` VA. 웹 파일:줄은 main `2fa9283` 기준. 화면 240×320.
> 스프라이트: `public/sprites/<폴더>/NNN.png` = PZX 이미지(코드의 종류 0), `…/frames/NNN.png` = 프레임(종류 1), 애니 = 종류 2.
> 반투명: 채우기 색 윗바이트가 알파 — `0x80RRGGBB` = 50%.

## 1. 구현 명세 — 게임에 넣을 것

### F-1. 알림·질문 창 (확정) — `src/shared/ui/MessageBox/MessageBox.tsx`, `.css.ts`
- **판**: #335FCD 채우기, 위·아래 5줄 선 = #1E1AA6 · #1E1AA6 · #FFFFFF · #7B9EFF · #7B9EFF. 상자 x = 0, 폭 240
- **글**: 흰색, x = 120 − 75, 폭 150, y = 상자y + 20 (줄 높이 14)
- **버튼 = `ui/popup.pzx` 프레임 그림** (글자 아님):
  | 창 종류 | 버튼 |
  |---|---|
  | 알림 | 프레임 0 "OK" (파랑) 하나 |
  | 예/아니오 | 비선택 1 "예"·2 "아니오" (파랑 41×15) / 선택 6·7 (주황 49×23). 처음 선택 = 예 |
  - "확인" 이 아니라 **"OK"**, 선택색은 노랑이 아니라 **주황 그림**
  - 선택 그림 6·7 원점 = (−4,−4) → 같은 기준점에 사방 4px 넘쳐 그려진다 (49 = 41+8, 23 = 15+8)
  - 창 종류 (점프표 0xd368c): **1 알림 · 2·4 예/아니오 · 3 버튼 없음 · 8 · 16** (8·16 쪽이 popup 프레임 3 "새로하기" · 5 "빠른실행" 으로 보임). *(검증 V4 정정: 노트의 "종류3→예/아니오, 종류4→0x75234" 는 한 칸 밀린 읽기)*
- **크기**:
  ```
  h = 줄수×14 + 50 + 행수×버튼높이(15) + (행수−1)×10
  상자 y = (320 − h) / 2
  버튼 x0 = 120 − (열수×폭 + (열수−1)×40) / 2, 열마다 +(폭+40)
  버튼 y = 상자y + h − 15 − (행수×높이 + (행수−1)×10)
  ```
  예) 한 줄 예/아니오: h = **79**, 상자 y = 120, 글 y = 140, 버튼 (59,169)·(140,169). 웹은 86
- **열림**: 그리는 높이 10 에서 시작, **매 틱 ×2** (10→20→40→79), 세로 가운데에서 위아래로 펼쳐짐. 펼치는 동안 글·버튼 안 그림
- **닫힘**: 매 틱 폭 **÷2** (240→120→60), 99 이하면 닫고 콜백
- **뒤 어둡게**: 켜진 첫 그리기에서 한 번, 화면 전체 검정 단계 5

### F-2. 외출 지도 (확정) — `src/pages/outing-map/ui/OutingMap.tsx`, `OutingMapScreen.tsx`
- 지도 (0,11), 건물 = 프레임 1~5 원점: 경기장(64,119) · 번화가(127,77) · 병원(0,84) · 학교(179,143) · 방송국(78,209) — 웹과 같음
- **고른 건물**: 흰 1px 테두리(단색으로 상하좌우 4번 찍고 위에 보통으로 한 번). 고르지 않은 건물은 효과 1·인자 7 (어둡게로 보임, 추정)
- **밤**(시간대 2 = 시각 20~5시): 화면 전체 단계 9 로 어둡게 한 뒤 **프레임 6(창문 불빛)** 을 그림. 고른 건물은 그 뒤에 그려 밝게 남음
  - 시간대: 6~15시 0 · 16~19시 1 · 그 밖 2 (같은 값으로 mode_back.mpl 팔레트 t−1)
  - 웹 `OutingMap.tsx:50` 은 프레임 6 을 늘 그린다("길 점선" 으로 봄) → 고칠 것
- **장소 이름**: img_text 프레임 [229,230,231,232,233], **1px 외곽선 글자**(8방향으로 테두리색 찍고 가운데 글색)
  - 보통: 검정 테두리 + 흰 글 / 고른 칸: 노랑(255,255,0) 테두리 + 검정 글
  - 이름 박스에는 mapY(11)를 **더하지 않는다** (원본 그대로면 이름만 11px 위 — 미해결)
- **선택 화살표**: 고른 프레임 박스 1, event_map 애니 0(프레임 7/8, 까딱) 기준점 (mapX + bx + bw/2, mapY + by). 웹은 2px 왼쪽·7px 아래
- **[!]**: 박스 2, 애니 2(프레임 11/12) 기준점 (mapX + bx + bw/2, mapY + by). 웹은 15px 아래
- **장소 기능 목록 = 말풍선** (웹은 전체 화면 목록):
  - 고른 건물 박스 3 (다섯 곳 모두 70×42), 판 #2E4694 둥글기 1
  - 칸 두 개: (x+4, y+4, w−8, (h−12)/2) = 62×15, 둘째 칸 y = 첫 칸 y + 15 + 4. 칸 바탕 #213473
  - 고른 칸: 노랑 1px 사각 테두리 + 노랑 글 / 안 고른 칸: 흰 글. 글은 가로·세로 가운데
  - 칸1 = StrMODE[48] "들어가기", 칸2 = StrMODE[49+장소](팬미팅·외식·입원·야구교실·CF촬영), 시즌모드는 [54+장소]
  - 비용·설명 글 없음

### F-3. 타석 HUD (확정) — `src/widgets/batting-stage/lib/renderHud.ts`
- 점수 글꼴 = `ui/num.pzx`, **오른쪽 정렬**(자간 −3), 박스 = game_ui 프레임 0 박스 1. 아래 줄은 y+3
- **새 램프 확대 연출**: S·B·O 파트 [13,14,15], 카운트가 바뀌면 그 램프를 **5배 → 1배로 5틱 동안** 줄이며 제자리에 (위치 = x − (z−1)·w/2). 웹 `renderHud.ts:14` "확대 연출 없음" 은 틀림
- 원본에 조준 커서 없음 (웹과 같음)
- 하늘·구름·펜스는 웹이 이미 원본대로

### F-4. 타이틀 (확정) — `src/pages/title/model/titleIntro.ts`, `ui/TitleScreen.tsx`
- main_title **애니 0**(인트로, 20틱)을 그대로 돌린다: 프레임 0(2) 1(2) 2(2) 3(1) 4(1) 5(1) 7 6 8 9 10 11 12 13 14 15 13. 선수 안착 = **6틱째**(프레임 3)
- 끝나면 **애니 1** 반복: 프레임 13(2틱) → 16(1, 흐린 "PRESS ANY KEY") → 17(4, 밝은) → 16(1). 이 빌드는 **PRESS ANY KEY** (TOUCH SCREEN 은 안 씀)
- 인트로가 끝난 뒤에만: 저작권 프레임 18 을 **(59,308)**, num 131 "2" 를 **(189,308)**, 전체이용가 이미지 7 을 **(198,2)**
- 웹: 옛 디코더 프레임 번호로 키프레임을 짬, TOUCH SCREEN 8프레임 깜빡임, 저작권 (55,306) 추정 → 전부 원본 애니로

### F-5. 메인 메뉴 = 화면 아래 반원 바퀴 (중심·원·각도 확정, 칸별 그림 유력)
- 중심 (120,320), main_ball 애니 0 이 중심에서 돈다
- 테두리 원 3겹: 반지름 93 RGB(37,55,120) · 95 RGB(138,185,235) · 97 RGB(36,55,120)
- 6칸 각도 [0,45,90,180,270,315]°, 한 칸 이동 5틱 (카운터×9°, 90° 벌어진 칸은 ×18°)
- 칸 위치: x = 120 + cos(a)·93 (+20 if a ∉ [90,270]), y = 320 + sin(a)·93. 보이는 칸: 180(27,320) · 270(120,227, 선택) · 315(≈206,254) · 0(233,320)
- 칸 아이콘 = img_text 프레임, 각도 구간별 main_ui 이미지 1~4 분기(뜻 미확인)
- 웹 `MainMenuScreen.tsx:67-71` 셀렉트박스는 **2026-09-13 사용자 요청으로 바꾼 것** → 되돌릴지는 결정 사항

### F-6. 훈련 팝업 (확정) — `src/shared/config/original/trainingAnimation.ts`
- 사각형 mode_ui 프레임 10 박스 0 = (0,65,240,72), 세로 보정 d: 관리 화면 0 (웹 (120,137) 맞음) · 외출 연출 창 59
- **장타형 동작표가 따로 있다**: 히트/모든능력치 0xd4a18 = [0,5,6,7,8,9,10,11,11,11,11,11] · 파워 0xd4a6c = [0,5,6,7,8,8,8,8,8]. 웹 `trainingAnimation.ts:35-36` 은 타격형 표(0xd49e8·0xd4a48)만 → 장타형 추가
- 투수편: 표 0xd49a4 (17칸), x = 가운데 − 0x34 = 68, y = 바닥 − 12, 단일 PZX 캐릭터(동작 번호 = 프레임 번호)
- 외출 연출 창(d=59): R 을 검정으로 채우고 위아래 11px 띠 #395DCE, 선 #294DAD · #4A7DFF

### F-7. 경기 결과 (확정) — `src/pages/game-result/ui/GameResultScreen.tsx:50-57`
1. 패배면 화면 전체 검정 단계 8 로 어둡게 (이기면 그대로)
2. 띠 fillRect(0, 40, 240, 30, `0x80304EA2`) = RGB(48,78,162) 50%
3. game_ui 프레임 8 (171×25 파란 막대) 을 (34, 35)
4. result.pzx 프레임 0 "YOU WIN" / 1 "YOU LOSE" 를 기준점 (120, 50). 무승부 그림 없음(패배 쪽)
5. 아래(유력): 미션 모드는 가운데 판 176×152, 그 밖은 (0,80) 점수판 + 판 176×202, "!R%d회"·"!C기록이 없습니다!"·"승리 추가 보상[…G포인트]"

### F-8. 공용 창·환경설정 (확정 — 줄별 좌표 미해결)
- 공용 창 0x55e60: #335FCD 둥근 판 + #080408 1px 테두리, 안쪽 흰 둥근 테두리(−3), #E2E2E2·#C5C5C5·#1D44A8 선, 윗부분 18px 제목 띠(유력). 켜면 먼저 화면 검정 단계 5
- 환경설정(종류 8): 화면 가운데 **192 폭** 창, 높이 212(작은 판 130) → (24, 54, 192, 212). 제목 그림 img_text 프레임 289, 항목 그림 img_text 이미지 39·87·38·20·98·53·102·89 · 프레임 35·37, 글 StrMAINMENU[63]~[78]
- 공용 UI 스프라이트 = **`ui/img_text.pzx`** (이미지 300개+)
- 웹 `src/pages/settings/` 는 PixelScreen+MenuList

### F-9. 속도 기본값 (확정) — `src/shared/config/frameRate.ts:6,14`
- 옵션 +0x2f = 2 → fps 표 [4,10,16,22,28][2] = 16fps = **62ms**. 웹 값 같음, 주석 "추정" → "확정(0x9f2a4)"
- 같은 생성자: 소리 크기 +0x2e = 2(×25 = 50), +0x3b = 1(진동 켬으로 보임), +0x2d = 0

## 2. 원본 버그 — 그대로 옮기고 나중에 수정 후보
- 외출 지도 장소 이름에만 mapY(11px)를 더하지 않는다 — 원본 버그인지 실기 화면 높이가 297 이었는지 미해결

## 3. 미해결
- 미션 선택·기록·스페셜·시즌 끝·상점의 그리기 함수 이름 맞추기와 줄별 좌표 (메인 메뉴 하위 상태 16~42 목록은 4절에 있음)
- 상점·장비 창 후보 0x81dc0 (mode_ui 프레임 33 박스 0~5)
- 메인 메뉴 칸별 그림 분기(main_ui 이미지 1~4)의 뜻, 효과 1·인자 7 / 효과 0x10 의 정확한 뜻

## 4. 상세 근거 (해독 노트 전문)

