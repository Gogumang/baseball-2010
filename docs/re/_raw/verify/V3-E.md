# V3-E 검증: docs/re/E-defense-rules.md (0절·1절·2절)

(요약은 끝에 채움)

## 항목별 결과

### E-1 연장 규칙
- ✅ **원본 이닝 0-기준 / 0xb6814 의 8**: `b6818 adds r1,#0x69; b681a movs r3,#8; strb` 확인. 0xb6798(초기화)이 `b67c0 adds r5,#0x6b; strb 0` 으로 이닝을 0 에서 시작, 점수판 0x6c..0x7d(0x12=18바이트=9회×2)를 비움. 0xb68fc `b6910: r3 = state+0x7f−0x16 = +0x69`, `cmp 이닝(+0x6b), [+0x69]; blt 안끝남` → 이닝 ≥ 8(=9회) 에서만 끝 판정. 미션 바이트3 하위 4비트 → +0x6b (0xaa688~0xaa69a, 99 상한) 도 확인. 0-기준 확정.
- ✅ **연장 보정 0xc13e0**: `ldrsb r2,[+0x6b]; cmp r2,#9; ble 건너뜀; cmp r0(세기),#0x32; ble 건너뜀; r2−=9; r1 = 세기 + r2*5; >100 이면 100` → 이후 `[sp+0x58]*3 ÷ trunc(세기/20)`, `[sp+0x54]*3 ÷ 같은 값` (0xc1400~0xc1426). 1절 식과 같음. (1절에 **세기 상한 100** 이 빠져 있으나 웹 `quickAtBat.ts:140` 에 `Math.min(100, …)` 이 이미 있어 문제 없음.)
- ✅ **스윙 강제 0xc26e0**: `rand(0,100) ≤ 0x3b(59)` 면 스윙, 아니면 `ldrsb [+0x6b]; cmp #0xe; bne → 0xc1818 투구 판정` / 같으면 0xc11f0 스윙. 0-기준 14 = 15회, 그 이닝만 — 맞음.
- ✅ **웹 한 칸 어긋남**: `quickAtBat.ts:41 FORCED_SWING_INNING = 14`, `:139 situation.inning > EXTRA_INNING_FROM`, `:140 (inning − EXTRA_INNING_FROM)*5`, `:276 situation.inning === FORCED_SWING_INNING`; `balance.json:187 "extraInningFrom": 9`; `quickAtBat.ts:81-82` 주석 "1부터. 10회부터 연장 보정"; 호출부 `simulateHalfInning.ts:72 { inning }`, `leagueDay.ts:56 for (inning = 1; …)`, `:57,:64` 로 1-기준 이닝을 그대로 넘김. → 웹 10회부터 +5, 14회 강제 / 원본 11회부터 +5, 15회 강제. 고칠 값(from=10, forced=15 또는 inning−1) 도 식에 대입해 맞는지 확인(웹 11회 → (11−10)×5=5 = 원본 0-기준 10 → (10−9)×5=5).
- ✅ **연장 상한·동점 종료 없음 (0xb68fc)**: 위 함수 전체(0xb68fc~0xb6986)에 이닝 상한 비교가 없고, 끝 조건은 모두 "점수 다름/홈 앞섬" 뿐 — 동점이면 r5=0. 이닝 증가 0xb6b6c 는 `아웃>2 && state[9]==1` 이면 `+0x6b += 1` 로 막는 값이 없음. 점수판 0xb6988 = `state+0x6c + 2×(이닝 mod 9)+side` (0xca911=mod, 인자 9) 확인. 웹 `leagueDay.ts:16 MAXIMUM_INNINGS = 14`, `:87-89` 동점이면 `recordLeagueResult(home, away)` = 홈 승 — 문서 인용 맞음.
- ✅ **경기 끝 판정 식** (0xb691e~0xb6946): r1=[+0x7e], r4=[+0x7f]. state[9]==1: 아웃>2 → r1≠r4 면 끝 / 아웃≤2 → r1<r4(홈 앞섬) 면 끝. state[9]≠1: 아웃≤2 → 안 끝 / 아웃>2 → r1<r4 면 끝. 1절 표와 같음. (+0x7f=홈 이라는 해석은 state[9]==1(말)에서 끝내기가 +0x7f 우세로 판정되는 모양과 맞음.)
- ✅ **콜드게임** (0xb6948~0xb697e): `ldrsb [+0x6b]; cmp #5; ble 안함` = 0-기준 >5 = 7회부터, 10점 차. 세부: 말·아웃≤2 → 홈이 10점 이상 앞설 때만 / 말·아웃>2 → 어느 쪽이든 10점 이상 / 초·아웃≤2 → 안 끝 / 초·아웃>2 → 홈이 10점 이상 앞설 때만 (원정이 초에 10점 앞서도 말까지 감). 1절 "10점 차" 는 요약으로는 맞음(웹 사람 경기 `gameState.ts:9-11` 에 이미 있음). 
  - 참고(문서에 없는 웹 차이): 웹 리그 하루치 `leagueDay.ts:56-69` 는 **콜드게임도, 말 공격 중 끝내기 중단도 없다** (`simulateHalfInning` 이 항상 3아웃까지 돈다 → 9회말 이후 홈 점수가 원본보다 커질 수 있음, 승패는 같음).

### 0절 "같은 것" 목록
- ✅ **미션 바이트 4 = 아웃|볼|스트라이크**: 0xaaf70 `ldrb [r6,#4]; ands #3 → strb [state,#6]`, 0xaaf78 `lsls #28; lsrs #30 → [state,#5]`, 0xaaf82 `lsls #26; lsrs #30 → [state,#4]`. 칸 뜻은 0x9d57c 에서 `[+4] cmp #1 ble → 1 / 5(삼진)`, `[+5] cmp #2 ble → 2 / 3(포볼)` 로 +4=스트라이크, +5=볼 확인. 웹 `tools/generate_game_data.py:450-451` (`outs: count & 3`, `balls: (count >> 2) & 3, strikes: (count >> 4) & 3`, MISSION_COUNT_OFFSET=4 는 :415) — 같음.
- ✅ **부호 난수 rand(0,2), X 먼저 Y 나중**: 0xbfaa0 = `movs r0,#0; movs r1,#2; bl 0xbfa54` (인자 없는 함수). 0xc144c 첫 호출 결과 0 이면 `[sp+0x58]` 부호 반전(0xc145e `rsbs`), 0xc1464 두 번째 호출이 다음 값. 웹 `quickAtBat.ts:147-148` (`=== 0` 이면 반전, X→Y) 같음. 인용 줄 146 은 주석 줄(실제 코드는 147-148, 사소).
  - 곁다리: 0xbfa54 LCG `s*0x343fd + 0x269ec3 >> 1`, a+s mod(b−a) 확인(a==b 면 a, a>b 면 b+s mod(a−b)).
- ✅ **상대 타순 이어짐**: 0xaf020 `[+0x291]=1; [+0x293] = ([+0x32]+1) mod 9` (0xca911, 인자 9) 확인, 0xaedee `strb r2,[r4]` 확정 지점 확인. 전체 디스어셈에서 `adds rX,#0x32` 뒤 strb 를 모두 훑으니 0x3a55e·0xaa88c·0xaa89e·0xb7c48 외에는 다른 객체(0x756a2 는 +0x21e 계열, 0xb777a 는 리그 구조체)뿐 — 이닝 전환에서 0 으로 되돌리는 곳 없음. 웹 `leagueDay.ts:57-66` 이 `awayOrder/homeOrder = nextBattingOrderIndex` 를 다음 이닝에 넘김 — 같음.
- ✅ **땅볼 진루타 판정**: 0xc15b8 `state[6] ≤ 1 → rand(0,10000) ≤ 0x176f(5999) → count(0xa9599)>0 → get(count−1)+0x78+0x14 ≤ 2 → 0xc0fb4(sim,1)`. 웹 `baseState.ts:31-38` (`outs<2 && !third && runnerCount>0`, 전원 한 루) 판정 같음.
- ✅ **안타 진루 += n, 4 이상 득점**: 0xc0fb4 가 주자마다 `루(+0x78+0x14) + n`, `>3` 이면 0xb6a9d(state, state[9]) 득점, +0x1d·+0x1e=1. 웹 `baseState.ts:64-84` 1·2·3루타 분기가 이 규칙과 같은 결과.

### E-2 희생플라이 없음
- ✅ **코드 0(0xc161c) 은 주자를 안 움직인다**: 0xc161c~0xc164c = `0xa7d0d(아웃 기록)`, `state[6] += 1`, `0xa8025(정산)`, `0xaf021(team,0)(다음 타자)`, 결과번호 1 → 끝. 주자 목록(sim+0x80)·0xc0fb4 호출 없음. 점프표 0xd9000[0] = 0xc161c 확인. 코드 3·6·7·8 도 진루타(0xc15b8) 뒤 같은 0xc161c 로 들어온다.
- ✅ **웹 희생플라이**: `baseState.ts:99-104` (`detail==='뜬공아웃' && third && outs<2 → runs 1`) 있음, `quickAtBat.ts:234` 가 코드 0 을 `'뜬공아웃'` 으로 내므로, `simulateHalfInning.ts:93 advanceRunners(…)` 를 거쳐 CPU 경기에 희생플라이 득점이 생김 — 문서 주장 맞음.

### E-3 땅볼 진루타 난수 순서
- ✅ 원본 0xc15b8: `state[6] ≤1` 만 보고 바로 rand(0,10000) → 그 뒤 주자 수·앞 주자 루 검사 (위 디스어셈). 웹 `simulateHalfInning.ts:92,96` 은 `canAdvance && randomIntegerBelow(...)` 로 조건이 맞을 때만 뽑음 — 문서 주장 맞음.
  - 추가 관찰(문서에 없음): 원본은 코드 6·7·8(번트)도 진루타 판정을 하고, 웹은 `detail === '땅볼아웃'` 만 보므로 코드 3 실패·기타 코드 폴백만 해당. 웹 `quickAtBat.ts:247` 은 "그 밖 코드" 를 모두 땅볼아웃으로 보내므로 코드 12 도 땅볼아웃이 된다(아래 E-4 참고).

### E-4 결과 코드 25칸 (점프표 0xd9000)
- ✅ `re u32 0xd9000 25` = [0]c161c [1]c170c [2]c170c [3]c150c [4]c170c [5]c170c [6..8]c15b8 [9]c164e [10,11]c170c [12]c1666 [13,14]c170c [15]c1680 [16,17]c170c [18]c1680 [19,20]c170c [21]c16b8 [22,23]c170c [24]c16f4. 디스패치 0xc14fc `cmp 코드,#0x18; bls 표 / 아니면 0xc170c` → 25 이상도 스트라이크 쪽(문서 "나머지" 와 모순 없음).
- ✅ 각 대상 확인: 3 = `rand(0,0x2710) < rand(0, 0xb570d(타자,3,…,0x5a,1))` 면 0xc1170(sim,1)+결과 9, 아니면 결과 3 → 0xc15b8 → 0xc161c. 9 = `[+4] ≤1 이면 +1`, 결과 0xd. 12 = 0xc1170(sim,1) (타자 주자 추가 0xa93ad + 0xc0fb4(sim,1)), 결과 8. 15·18 = `rand(0,0x29a=666) < rand(0,주력)` 면 코드+3 → 0xc16b8 `(코드−0xf)÷3 + 1` → 0xc1170. 21 = 0xc16b8 직행(3루타). 24 = 0xc1054 (홈런). 나머지 0xc170c = `[+4]+1; >2 면 [+6]+1, 0xa7c4d(삼진)`.
- ✅ "간이 엔진은 한 타석 아웃 1개" : 0xc161c·0xc170c 모두 `state[6] += 1` 한 번.
  - 참고(웹 대조, 문서 주장 아님): 웹 `quickAtBat.ts:246-247` 은 코드 12 를 "번트 코드" 로 보고 땅볼아웃으로 처리한다. 원본 0xc1666 은 **1루타**. 간이 타석에서 `swingResultOf` 가 코드 12 를 낼 수 있다면 웹이 틀린 것 — 확인 권장.

### E-5 CPU 경기 도루
- ✅ **표 0xd9064**: `re u32 0xd9064 10` = [1,5,10,15,20,30,40,50,60,80] (0xc1994~0xc19a6 이 u32 10개를 스택 sp+0x18 로 복사). 웹 `entities/game/model/steal.ts:16` 과 같은 값.
- ✅ **식** (0xc1982~0xc1ae2): 주자 수(0xa9599) >0 → 가장 앞선 주자 get(n−1) 의 선수(0xb8b99) → 0xb570d(…,3,…,0x5a,1) 주력, `>0x3e7 이면 999` → `lead+0x78+0x14 == 3 이면 끝` → `rand(0,0x2710) < 표[주력÷100]×100` 이면 주자마다 도루 +1 후 0xc0fb4(sim,1) 로 전원 +1루. 실패 분기 없음, 투수·포수 능력 안 씀 — 1절 의사코드와 같음.
  - 잔 차이(사소): "각 주자 도루 +1" 은 무조건이 아니라 주자 +0x1e==0 이고 0xa56dd(ctx,선수,0)≠0 일 때만 선수 +0x2c(strh)+1, 경기 기록 +0x15 +1 (0xc1a6c~0xc1aba). 전원 +1루 자체는 조건 없이 0xc1ad4.
- ✅ **투구 판정 경로 뒤에만**: 0xc1818 의 판정 점프표 0xd908c(1~13) 모든 갈래(스트라이크 0xc18ca, 볼 0xc18d4, 포볼/데드볼 0xc1918, 삼진 0xc18ec, 13 → 0xc18de, 6~12 → 바로)가 0xc1982 로 모인다. 스윙 경로 0xc11f0 에는 이 표 참조 없음(0xd9064 참조는 전 바이너리에서 0xc1994 하나).
- ✅ 웹 간이 엔진에 도루 없음: `quickAtBat.ts`·`simulateHalfInning.ts`·`leagueDay.ts` 에 steal/도루 참조 없음.
- 참고(문서에 없는 웹 차이, 유력): 같은 0xc1818 의 **포볼·데드볼(판정 3·4 → 0xc1918)** 은 타자 주자를 넣고(0xa93ad) **0xc0fb4(sim,1) 로 모든 주자를 한 루씩 민다** — 밀려나지 않는 주자까지 간다. 웹 `baseState.ts:87-92 advanceForWalk` 는 밀려난 주자만 진루. (0xa93ad 가 새 주자 루를 0 으로 둔다는 점은 1루타 경로 0xc1170 과 같은 호출이라 그렇게 봄 — 내부 미확인.) 또 삼진·포볼로 3아웃/타석이 끝난 뒤에도 같은 투구에서 도루 판정이 이어서 돈다.

### E-6 CPU 투수 교체
- ✅ 0xc262c 루프 `c2662: bl 0xc1ba4` 확인.
- ❌ **"특정 모드(state[1]==3)에서 7회에 마선수로 교체"** — 0xc1c24 `ldrsb [state+0x6b]; cmp r3,#7; bne` 는 **0-기준 이닝 7 = 8회**다 (E-1 에서 문서 스스로 확정한 0-기준). 4절(3c) 도 "7회" 라 옮겨 적기 실수가 아니라 해석 실수. 조건 자체(state[1]==3, 0xb6ded(state)==2, state[0x31+state[0xa]]==0, 현재 투수 비마선수 0xb6389, 벤치 team+0x33 명 중 첫 마선수 0xb8b09→0xb6389)는 맞음.
- ⚠️ **"그 밖: 0xac428"** — 맞으나 빠진 것: 모드 3 이라도 8회(0-기준 7)가 아니거나 마선수를 못 찾으면 같은 "그 밖" 길로 떨어진다. 또 0xac428 앞에 **0xac228(ctx, 공격팀 [sim+0x74], 주자, state)** 를 먼저 부르고(결과 r6 을 반환값에 OR), 0x66864 가 거짓이거나 `[sim+0x94]>0 && state[1]==3 && 현재 투수가 마선수` 면 0xac428 을 건너뛴다 (0xc1c4a~0xc1cb8). 0xac228 은 문서에 없음(공격측 교체 AI 로 보임, 미확인).
- ❌ **0xac428 이닝 구간이 한 칸 어긋남**: r6 = `ldrsb [state+0x6b]` (0xac4d4) 로 **0-기준 이닝**. 실제 분기(0xac526~0xac574): `r6 ≤3` → B>4 교체 / `r6==4` → B>4 && s≤49 / `r6==6` → B>2 && s≤29 / 그 밖(`r6==5` 또는 `≥7`) → A>1 / 마무리 판정은 `r6 > 7`. 1-기준 회로 바꾸면 **1~4회 · 5회 · 7회 · 6회와 8회 이상 · 마무리 판정 9회 이상**. 1절·4절 모두 "이닝 ≤3 / 4회 / 6회 / 5·7회+ / 8회+" 로 0-기준 숫자를 회로 적었다 (웹에 지적한 것과 같은 종류의 실수).
- ✅ 나머지 조건: 특수 투수 0xb633d → `A>2 || B>3 || s≤0x27(39)` 교체, 아니면 안 바꿈. 역할(0xb6ded→0xb6705) <0 → 없음 / 0·1 → `A>2 || s≤0x13(19)` 교체 / 2 → `A≠0 && 리드+2 ≤ 0` 교체. 리드 = score(state[0xa]) − score(state[9]) (0xb69b1) = 수비−공격. 마무리: `0<리드≤5` 이고 (`리드≤3` 또는 `리드 ≤ 주자수+2`). state[+0xd]≠0 이면 안 바꿈, 교체 뒤 `strb 1,[state+0xd]` (0xac652). A = 0xa61b8(ctx,0,1) = team[state[0xa]]+0x27c+8 = +0x284 (세 번째 인자 1 = 수비측 state[0xa]) 확인. 체력% 0xaebb0 = 모드 7 이면 100, 아니면 투수 +0x2c ÷100.
  - 잔 차이: "벤치 수 ≤ 최소 벤치" 의 인자는 0xc1ba4 가 넘기는 **마선수 벤치 번호(못 찾으면 0)** 다 (0xc1c82~0xc1c8a → [sp+0xc]). 일반 경기에서는 0 이라 "벤치가 비었으면" 과 같음. 또 벤치 ≤1 이고 벤치 0번이 마선수이며 모드 3 이면 안 바꾸는 조건(0xac458~0xac484)이 빠져 있음.
- ❌ **새 투수 고르기 방향이 반대**: 0xac5d8~0xac61c: `0xb8a8d(team,0)` 이 참이고 **마무리 상황이 아닐 때** 0xac360(ctx,주자,state) 검사 → 참이면 벤치 마지막(벤치수−1), 거짓이면 벤치≤1 이면 0번 / 아니면 0xabfcc. **마무리 상황이거나 0xb8a8d 가 거짓이면 0xabfcc(ctx, team, 이닝, 모드3플래그, [sp]=마무리플래그)**. 문서 "마무리 상황이면 벤치 마지막, 아니면 0xabfcc" 와 거꾸로다(4절 3c 도 같음). 마선수 제외(모드 3 플래그)면 고른 선수가 마선수일 때 다시 고름(0xac626~0xac640) — 맞음.
- ✅ 웹 선발 고정: `leagueDay.ts:49-50 startingPitcherOf`, `simulateHalfInning` 은 pitcher 고정 인자. `quickAtBat` stamina 는 입력값 그대로.
- ⚠️ "투구마다 +0x28 증가, 체력 소모", "team+0x27c 투구 수(0xa5e5e)" 는 이번에 확인하지 않음(1절은 "유력" 표기).

### E-7 투수 미션 조준점 흔들림 (0x39c5c)
- ✅ **조준 이동·자르기**: `x(+0x10b8) += [+0x10b0]×0x14`, `y(+0x10bc) += [+0x10b4]×0x14` (0x39c5e~0x39c8c). 중심 = 표 0xcfbcc[`ldrsb [+0x17e1]` ×12] (`re u32 0xcfbcc 6` = (19415,1202,29705)·(20585,1202,29705)), x 는 중심 ±0x258(600) (`0xfffffda8`, `0x96<<2`), y 는 ±0x190(400) (`0xfffffe70`, `0xc8<<1`) 로 자름. 1절 식 맞음.
  - 빠진 것(사소): 세 번째 좌표 `[+0x10c0] −= dy×10` 도 중심+8 기준 ±200 으로 자른다(0x39c8a~0x39d1a). 흔들림은 자르기 **뒤**에 더하므로 그 틱에는 ±600/±400 을 최대 80 넘을 수 있다.
- ✅ **조건**: `[화면+0x1788] ≠ 0 && [화면+0x1104] == 5` 일 때만 (0x39d18~0x39d2c), `switch [ctx+0x20]`.
- ✅ **코드별 식**: 1 → `rand(0,100) ≤ 0x32` 면 `x += rand(−0x28, 0x28)`; 2 → `≤50` 면 `x += rand(−0x50,0x50); y += rand(−0x50,0x50)`; 3 → `≤50` 면 `x = rand(중심x−600, 중심x+600); y = rand(중심y−400, 중심y+400)` (0x39d44~0x39dde). 1절과 같음.
- ✅ **호출·조건코드 적재**: 0x52e54 `bl 0x39c5c` (전 바이너리 유일 호출). 0xaa6c2 `ldrb r3,[r0,#0xd]` → 0xaa6c8 `str r3,[r1,#0x20]`.
- ✅ **붙은 미션**: 웹 `missions.ts` 에서 conditionCode≠0 은 투수 4·5 = 1, 8·12 = 2, 10 = 3 뿐(타자 전부 0) — 1절과 같음 (문서의 "로제·크라이져·킹타이거" 는 상대 이름으로 보이며 웹 미션 이름은 각각 '착...착각하지마!!'·'영혼의 사우팅을 들려줘'·'최강의 챔피언'). 생성기 `MISSION_CONDITION_OFFSET = 13` (generate_game_data.py:394). 웹에서 conditionCode 를 읽는 곳은 타입 선언(missions.ts:25) 외 없음 — "읽기만 함" 맞음(사실상 읽지도 않음). `src/pages/pitching` 존재.
