# 2차 해독 공통 지침 — _BRIEF.md 를 먼저 읽고, 아래가 다른 점이다

## 결과 파일 위치 (바뀜)
- 결과 파일은 **`/Users/evan123/Desktop/code/baseball-2010-re/docs/re/_raw/notes/<이름>.md`** 에 쓴다 (이 worktree 브랜치는 문서 전용이라 써도 된다).
- 그 밖의 저장소 파일(src/, tools/, docs/re/ 의 다른 파일, 원래 저장소 baseball-2010/)은 **고치지 말 것**. git 명령 금지.
- 임시 파일은 스크래치패드(`/private/tmp/claude-501/-Users-evan123-Desktop-code-baseball-2010/a8d7dac6-6002-4b07-af77-039dbed151f1/scratchpad/work/<이름>/`)에.

## 끊겨도 이어서 하기 — 체크리스트
1. **시작하자마자** 결과 파일을 만들고 맨 위에 `## 체크리스트` 를 쓴다. 맡은 항목을 잘게 나눠 `- [ ] 항목` 으로 적는다.
2. **결과 파일이 이미 있으면** 체크리스트를 읽고 `[x]` 가 아닌 첫 항목부터 이어서 한다 (다시 처음부터 하지 말 것).
3. 항목을 시작하면 `[~]`, 끝나면 `[x]` 로 바꾸고 그 항목의 결과 절을 **바로** 덧붙여 저장한다. 풀 수 없으면 `[-]` 와 이유.
4. 체크리스트 아래에 `## 요약` 을 두고 끝날 때 채운다.

## 다른 주의
- 셸의 grep·find 함수가 깨져 있다 → `/usr/bin/grep`, `/usr/bin/find`.
- 긴 디스어셈은 60줄 이하로 나눠 뽑는다. 한 번의 응답을 짧게.
- **공 궤적(투영·베지어) 코드는 해석하지 말 것** — 안전 필터로 막힌 주제다. 궤적은 원본 데이터(pitch.zt1)를 그대로 쓰기로 했다.
- 이미 풀린 것(`/Users/evan123/Desktop/code/baseball-2010-re/docs/re/*.md`)을 먼저 읽고 거기서 출발한다. 같은 것을 다시 풀지 말 것.
