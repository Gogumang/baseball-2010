import { style } from '@vanilla-extract/css'

export const tabs = style({
  display: 'flex',
  gap: '4px',
  flex: 'none',
})

export const compactTab = style({
  padding: '4px 0',
  fontSize: '10px',
})
