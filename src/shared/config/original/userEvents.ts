// 이 파일은 tools/generate_game_data.py 가 원본 패키지에서 생성했다.
// 직접 고치지 말고 생성기를 고칠 것.

import data from '@/shared/config/original/data/userEvents.json'

/** 경기 후 평가·목표·연속 기록 문구 (원본 StrUSER_EVT, 114개) */
export const ORIGINAL_USER_EVENTS: readonly string[] = data
