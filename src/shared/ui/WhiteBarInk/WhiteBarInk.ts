import type { CSSProperties } from 'react'

/**
 * **흰 막대 위 글자 보정** — slt_frame **이미지 116**(57×15, 주요색 231,227,231) 위에 얹는
 * `img_text` 글자 프레임(흰색 239,239,239)에만 쓴다.
 *
 * ⚠️ **근사다 — `.mpl` 팔레트 미이식 때문에 넣은 임시 처리.** 팔레트를 옮기면 지운다.
 *
 * 원본은 `img_text.mpl` 팔레트를 갈아 끼워 글자 색을 바꾸는데 웹판에 팔레트가 아직 이식되지
 * 않았다. 그대로 겹치면 흰 글씨가 흰 막대에 묻혀 안 보이므로 글자만 뒤집어 읽히게 한다.
 * 파란 막대(이미지 117) 위 글자는 원본도 흰 글씨가 제대로 읽히니 이 보정을 쓰지 않는다.
 *
 * 쓰는 곳은 네 화면이다 — 선공/구장(A·B) · 경기정보(A·B·가운데 줄 딱지) · 마선수선택(A) ·
 * 스페셜 명예의전당(A). 경기정보 줄 딱지가 쓰는 **이미지 18**(39×15)도 116 과 같은 흰 막대다.
 * 팔레트를 이식하면 **이 파일을 지우고 쓰는 쪽에서 `style` 한 줄씩만 떼면 된다.**
 */
export const WHITE_BAR_INK: CSSProperties = { filter: 'invert(1)' }
