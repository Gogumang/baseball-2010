# 원본 이식 진행 상황

원본: `base/게임빌2010프로야구/0002C663.jar` (WIPI, 2009-09-29, LG텔레콤)

## 도구

```bash
python3 tools/extract_wipi_game.py base/게임빌2010프로야구/0002C663.jar -o base/extracted
python3 tools/decode_pzx.py base/work/jar -o base/sprites   # PNG 7,570장 (파트 3,426 + 합성 프레임 4,144)
python3 tools/generate_game_data.py                          # src/shared/config/original/*.ts
python3 tools/sprite_sheet.py <폴더> <출력.png>              # 확인용 시트
```

## ★ 가장 중요한 자료: 게임 내 설명서 (StrHOWTO)

원작이 자기 시스템을 스스로 설명한다. 추측하지 말고 여기를 먼저 볼 것.

- `StrHOWTO[11]` **관리 메뉴**: "2경기마다 관리 메뉴가 발생하며 다음 커맨드로 선수를 육성합니다.
  [선수정보] [트레이닝] [휴식] [외출] [아이템] [다음경기]"
- `StrHOWTO[14]` 목표설정 5가지 / 돌발미션 / **부상·질병은 사기가 낮으면 확률 상승**,
  관리메뉴 좌측 상단에 아이콘 표시
- `StrHOWTO[15]` 닉네임과 스킬. 스킬 색: **파랑=플러스, 빨강=마이너스, 보라=스페셜**(특수 외출 이벤트)
- `StrHOWTO[16]` **외출 커맨드는 2010에서 신규 추가**. 전체 맵으로 이동.
  `[!]` 아이콘 장소에서 `[들어가기]` → 특별 이벤트 (무시해도 페널티 없음)
  장소별 기능(타자편): 경기장=팬미팅 / 번화가=외식 / 병원=입원 / 학교=야구교실 / 방송국=CF촬영
- `StrHOWTO[22]` 투수편: 경기장=친선경기 / 방송국=구단CF
- `r_event_txt[172~176]` 트레이닝·휴식·외출은 **한 번에 한 가지만**. 트레이닝/외출은 사기가 떨어짐
- `r_event_txt[249]` "**하단 4번째에 위치한 [외출]** 커맨드"
- `StrITEM[150~154, 220~224]` 장소별 효과 수치

## 완료

- [x] 스토리 1770줄 → 152장면, 마크업 파서
- [x] 데이터: 팀 15(실명+로고), 타자 180, 투수 120, 마선수 10, 칭호 128, 스킬 80,
      아이템 255, 엔딩 22, 팁 74, 필살기 11
- [x] **PZX 142개 전부 해독, 실패 0** → PNG 7,570장 (파트 3,426 + 합성 프레임 4,144)
- [x] 마선수 10명 전원 애니메이션, 경기 등판
- [x] **관리 메뉴를 원작 6개 커맨드로** (2경기 주기)
- [x] **외출 지도 화면** — event_map 도시 지도 + 병원·번화가·경기장·학교·방송국
- [x] **스탯을 원작 구성으로**: 인기도·평판·사기·소지금(만원)·부상·질병
- [x] 도트 확대를 정수배로 고정 (깨져 보이던 원인)

## 미션 모드 (완료)

원작 설명서 `StrHOWTO[27]`: "타자편, 투수편으로 나누어지며 총 28개의 미션이 준비되어
있습니다. 육성 선수 혹은 팀을 조작하여 제한 조건 내에서 게임 내 목표를..."

- **미션 38개** — `XlsBATTER_MISSION` / `XlsPITCHER_MISSION` 안에 이름·목표·설명문·
  제한시간이 CP949로 박혀 있었다 (스토리 14×2 + 마선수 공략 5×2 = 설명서의 28개와 일치)
- **제한 조건은 설명문에서 읽는다** — 지어낸 값이 아니다
  - 목표 개수: "2안타와 1타점을 올리자!" → 안타 2, 타점 1
  - 타석 제한: "3타석 내에", "2타석 내에", "3번의 스윙으로"
  - 투구 수 제한: "10개의 공으로 2삼진을 잡아라!"
  - 제한 시간: 레코드의 timeLimitSeconds (사이클링 히트 120초 등)
  - 표기 차이는 별칭으로 흡수 (목표 "탈삼진" ↔ 설명문 "2삼진")
- **단계 잠금** — `StrMAINMENU[60]`: "오픈 되지 않은 미션입니다. 이전 단계를 클리어해주세요"
- **번트** — 배트를 대는 것이라 코스 허용 범위가 넓고 타이밍이 관대하며 타구는 약한 땅볼.
  Shift 또는 길게 눌렀다 떼기(모바일)
- **투수편** — 원작 `<투구 조작>` 그대로: 1. 구질 선택 2. 코스 선택 3. 투구 결정(게이지).
  "PERFECT로 결정됐을 시 더욱 강한 공" → 제구 +25, 구속 ×1.08
  구질 21종은 `binary.mod` 오프셋 855904의 이름 테이블 원본
  (FASTBALL·TWO-SEAM·H.FAST·SINKER·SHOOT·SLIDER·CURVE·FORK·CHANGEUP·CUT FAST·
   R.FAST·H.SINKER·H.SHOOT·H.SLIDER·S.CURVE·S.CHANGEUP·GYRO·P.SINKER·P.SLIDER·
   KNUCKLE·SPECIAL). 변화량·구속 수치는 이름에서 유추한 값 (원본 수치는 미해독)
- 투수 목표: 아웃 · 탈삼진 · MAX게이지 · 삼진콤보 · 무실점 · 노히트노런 · 퍼펙트게임
  (무실점/노히트노런/퍼펙트게임은 허용하면 0으로 되돌아가는 연속 조건)
- **이닝 단위 미션** — "5이닝을 노히트노런으로", "6이닝을 퍼펙트로"에서 이닝 수를 읽어
  3×이닝 아웃을 잡으면 성공. 안타를 맞으면 노히트노런 실패, 볼넷은 퍼펙트게임만 실패
- **도루** — 원작 미션 "번트와 도루를 1개씩". 능력치 설명의 "주루 : 주루, 수비 이동 속도"를
  따라 주루가 성공률을 좌우하고 투수 구속이 깎는다

## pitch.zt1 (구질 궤적) — 해독 완료

```
u8 항목수(22) | u32 오프셋 × 22 | 항목
항목   = u8 구질번호 | u8 레코드수 | u16 레코드크기 | 레코드 × 레코드수
레코드 = u16 크기 | u8 단계 | u16 점개수 | (i32 x, i32 y, i32 z) × 점개수
검산: 1 + 22*4 = 89 = 첫 오프셋,  2 + 레코드수 × 레코드크기 = 항목길이
```

**궤적 베지어 제어점이다.** 첫 점은 릴리스 `(-499, 1110, 0)`, 마지막은 플레이트
`(0, 1242, 5205)` 로 모든 구질이 같고, **가운데 점이 직선에서 벗어난 만큼이 변화량**이다.
직구는 점이 둘뿐이라 변화가 0 — 원본이 실제로 그렇다. y는 아래가 양수라 부호를 뒤집는다.

야구 상식과 정확히 맞는다: CHANGEUP 낙차 최대(1614), FORK 급강하(957),
SLIDER 왼쪽(-771), SHOOT 오른쪽(457), R.FAST 떠오름(-723).

구속은 레코드 수 비율로 계산한다 (직구 32개 = 1.0 기준).
**존 크기만 미해독**이라 좌표를 존 단위로 바꾸는 나눗수(1793) 하나만 우리가 정했고,
구질 21종 사이의 상대 비율은 전부 원본 그대로다.

## 해독한 포맷

```
.zt1        u32 파일크기 | u32 해제크기 | zlib
GST 문자열   'GST'+버전 | u16 항목수 N | u16 오프셋 N+1개(절대) | 데이터
GXL 레코드   'GXL'+버전 | u16 행크기 | u16 열수 | u16 행수 | u8[열수] 타입 | 행
            ※ 10 + 열수 + 행수*행크기 == 파일크기 로 반드시 검산
이벤트 텍스트 u32 항목수 | u32 데이터크기 | u32 오프셋 N개(상대) | 데이터

PZX  'PZX'+버전 | u32 이미지섹션시작 | u32 색상수 | u32 16 | u8 a | u8 변형
     프레임 블록(zlib): 프레임 = u16 파트수 | 파트
     파트 = u16 이미지번호 | i16 x | i16 y | u8 플래그
            플래그가 0이 아니면 추가 바이트가 붙는데 길이가 일정하지 않다.
            같은 파일 안에서도 다르므로 블록 끝에 정확히 닿는 조합을
            역추적(위치별 메모이제이션)으로 찾는다.
     이미지섹션: u8 비트깊이 | u16 이미지수 | u8 팔레트색상수 | RGB565 팔레트
                 | u32 압축크기 | zlib
                 ※ 팔레트색상수가 0이면 **이미지마다 자기 팔레트**를 앞에 단다:
                   u8 색상수 | u16 RGB565 × N | 보통의 이미지 헤더
     이미지 = u16 폭 | u16 높이 | u8 플래그 | CD CD CD | u32 데이터크기 | u32 0
     픽셀 = 행 RLE. 0xFFFF 이미지끝 / 0xFFFE 행끝 /
            최상위비트 1 → 리터럴 런 / 그 외 → 투명 건너뛰기
MPL  u8 ? | u8 팔레트수 | u32 오프셋×N | (u8 색상수 | u16 RGB565 × N)
     0번 색은 항상 #FF00FF 투명
```

## 화면 크기가 알려주는 것

240×320 전체 화면 그림은 `main_title/000` 하나뿐. `event_map/000`이 240×297로
거의 전체 화면 → **육성 화면은 도시 지도**. `mode_back/000`은 240×84 띠라 배경이 아니다.

## 화면 배치 — 무엇이 확정이고 무엇이 추정인가

서브에이전트 조사(`layout-re`)로 **확정된 부정적 사실**:
~~`ui/event_map.pzx` 등은 프레임 합성 데이터가 0개~~ — **틀린 기록** (2026-09-17 layout-re): event_map 13·mode_ui 92·game_ui 95 프레임에 파트·박스 배치가 있다.
(아래 원래 기록) 즉 지도 건물·메뉴 아이콘·HUD의 배치 좌표는 파일에 없고
`binary.mod`(890KB, 심볼 없는 ARM Thumb) 코드 안에만 있다. 파일에서는 복원 불가능하다.

| 화면 | 배치 근거 | 확실도 |
|---|---|---|
| 타이틀 | 배경의 투명 구멍 크기가 겹칠 스프라이트와 픽셀 단위로 일치 (137×114@(4,144)=로고, 87×241@(126,34)=선수) | **확정** |
| 메인 메뉴 6모드 | `main_ui` 글자 스프라이트 + 해외판 공식 설명의 6개 모드와 일치 | **확정** (항목), 배치는 추정 |
| 관리 메뉴 6커맨드 | 게임 내 설명서 `StrHOWTO[11]` 원문 | **확정** (항목), 배치는 추정 |
| 외출 장소·효과 | `StrHOWTO[16][22]`, `StrITEM[150~154]` 원문 | **확정** (내용), 좌표는 추정 |
| 지도 건물 좌표 | 없음 — 코드에만 존재 | **추정** |
| 경기 HUD 좌표 | 없음 — 코드에만 존재 | **추정** |

`tools/find_overlay_slots.py` 로 투명 구멍 기법을 자동화해 뒀다. 전체를 훑은 결과
이 기법이 통하는 배경은 타이틀 하나뿐이었다.

## 모듈 구조 (FSD)

바깥 레이어가 안쪽만 import 한다: `app → pages → widgets → features → entities → shared`.

```
src/
  app/       App.tsx · main.tsx · styles/{theme.css.ts, legacy.css}
  pages/     화면 14개 (create-player, game, management, pitching, record, …)
  widgets/   batting-stage — 캔버스 타석 합성
  features/  play-at-bat · play-game — 한 판을 굴리는 규칙
  entities/  at-bat · batting · career · game · mission · pitching · story (도메인 모델)
  shared/    ui · lib · api(포트) · config(원본 추출 데이터)
```

도메인 규칙은 `entities/*/model`에 두고 포트(`shared/api`)로만 바깥과 닿는다.
FSD 슬라이스 안에서 포트&어댑터를 유지한 것이라 둘이 충돌하지 않는다.

### 공통 디자인 컴포넌트 (`shared/ui`)

바깥 레이어는 배럴 하나만 import 한다 — `import { Panel, Hint } from '@/shared/ui'`.
파일 경로가 바뀌어도 호출부가 깨지지 않게 하려는 것이다.

| 컴포넌트 | 쓰임 |
|---|---|
| `PixelScreen` | 피처폰 화면 한 장 (타이틀바·본문·소프트키) |
| `Panel` | 테두리 상자. `heading` 없이 내용만, 내용 없이 제목만도 된다 |
| `Hint` / `Notice` / `BigResult` | 조작 안내 · 설명 한 줄 · 결과 큰 글자 |
| `StatGrid` | 이름·숫자 짝을 3열로 (`entries` 배열) |
| `MenuList` / `IconMenu` | 세로 메뉴 · 아이콘 격자 |
| `StatusBar` / `AbilityBars` | 소지금·사기 등 상태 줄 · 능력치 막대 |
| `DialogueBox` / `MarkupText` | 대사창 · 원본 이벤트 텍스트 마크업 해석 |
| `TitleTag` | 획득 칭호 딱지 |
| `PixelNumber` / `HeadingSprite` / `PopupLabel` | 원본 스프라이트 숫자·제목·팝업 |

미션 목표 막대(`GoalBar`)는 타자편·투수편 두 화면만 쓰므로 `entities/mission/ui`에 둔다.

### 디자인과 기능은 항상 다른 파일에 둔다

- 스타일은 **vanilla-extract `*.css.ts`** 로만 쓰고 컴포넌트 파일 옆에 둔다.
  일반 `.css` 파일은 하나도 남기지 않았다 (`legacy.css` 666줄 → 0).
- 색·치수는 `app/styles/theme.css.ts` 토큰을, 전역 규칙은 `app/styles/global.css.ts`를 쓴다.
- `.tsx`의 인라인 `style={{}}`은 **데이터에서 나온 값만** 남긴다
  (스프라이트 좌표, 게이지 폭, 능력치 막대 길이). 고정 디자인은 전부 `.css.ts`로 갔다.
- vanilla-extract는 `style()` 안에서 자손 선택자(`& img`)를 막는다.
  부모 기준(`${parent} &`)으로 뒤집거나 `globalStyle()`을 쓴다.

### 파일 크기 규칙

**`.tsx`는 150줄을 넘기지 않는다.** 넘으면 상태·규칙을 `model/`의 훅으로, 화면 조각을
별도 컴포넌트로 뺀다. 실제로 이렇게 갈랐다:

| 원래 | 나눈 결과 |
|---|---|
| `App.tsx` 660줄 | `ui/{App,EntryRoutes,CareerRoutes,MissionRoutes}.tsx` + `model/{screen,useAtBatRunner,useCareerSession,useMissionSession}.ts` |
| `BattingStage.tsx` 346줄 | `ui/BattingStage.tsx` + `model/{stageRefs,useStageAnimation,useStageControls}.ts` + `lib/stageText.ts` |
| `PitchingScreen.tsx` 172줄 | `ui/{PitchingScreen,CourseGrid,PitchGauge}.tsx` |

## 스프라이트는 전부 배포한다 (2026-09-13 수정)

한동안 `tools/prune_sprites.py` 가 **소스에서 문자열로 참조하는 것만** public 에 복사했다.
그 결과 142개 폴더 중 27개만 남고 115개가 빠져 화면이 원작과 달라 보였다.
빠졌던 것: `attack_sky_cloud`(하늘), `batter_helmet`·`batter_shadow`(타자 레이어),
`item_bat_*`(배트·장비), `pitcher`(평범한 투수), `defender`(수비수),
`event_char_0~2`(이벤트 인물), 각종 `*_effect`, `trainning`, `result`, `ending`.

지금은 `base/sprites` 전체(7,047장 · 48MB)를 그대로 `public/sprites` 에 둔다.
용량보다 원작 재현이 우선이다. prune 은 더 이상 기본 경로가 아니다.

## 타자·투수는 한 장이 아니라 레이어다

원작 타자는 파일 하나가 아니라 여러 PZX 를 겹쳐 만든다. 뒤에서부터:

```
batter_shadow → item_bat_leg_0 → batter_balancer → item_bat_hand → batter_helmet → item_bat_head_0
```

`batter_balancer` 만 그리면 **머리도 배트도 없는 몸통**만 나온다 (실제로 그랬다).
타자 타입별 몸통이 따로 있다: `batter_balancer`(밸런스) · `batter_batter` · `batter_sluger`.

### origins.json — 레이어를 겹치려면 원점이 필요하다

`decode_pzx.py` 는 프레임을 파트들의 바운딩 박스로 잘라 저장한다. 그래서 레이어마다
크기가 달라 그냥 겹칠 수 없다. 잘라낸 원점(앵커 기준 상대 좌표)을
`frames/origins.json` 에 같이 남기도록 고쳤다. 렌더러는 앵커 한 점에 모든 레이어를
`anchor + origin` 으로 찍는다.

## 타석 배경 배치 (2026-09-17 원본 좌표로 교체 — position-re 역분석)

binary.mod 카메라: 월드 480×390 을 centerOn(240, 390 − H/2) → H=320 이면 화면 오프셋 (−120, −70).
`widgets/batting-stage/lib/stageLayout.ts` 에 모았다 (전부 바이트 확인, side1=반전 없음 기준):

| 요소 | 화면 좌표 | 근거 |
|---|---|---|
| 그라운드 attack/000 | 위 y=156, 왼쪽 x=−193 (156+164=320, 흙 늘리기 불필요) | 0x7725c |
| 타자 앵커 | (175, 281) | 표 0xcfb2c |
| 투수 앵커 | (83, 188) | 표 0xcfb18 |
| 존 표시 | slt_pitch 프레임 73 을 존 (101,240,33,33) 가운데에 | 표 0xcfb7c, 0x35ae8 |
| 판정 글자 | 중심 (117, 224) | 0x39504 |
| 공 크기 | ball.pzx 11장 묶음(2→12px), 원본은 "크기 + 11×구질" | 0x358fc |

예전 웹판은 그라운드를 y=40 에 그려 화면 전체가 116px 위로 올라가 있었고, 존이 68×84 선 사각형이었다.
**추정으로 남은 것**: 화면 높이 320(400 기기면 y+70), side0(좌우 반전) 조건, 공 릴리스 (86,164)·궤적(원본은 3D 투영 0x35854),
y<156 의 하늘·펜스(미해독이라 하늘색+구름), 커서 모양·HUD 좌표(0x4c4bc).

## 관리 커맨드는 하단 한 줄이다 (2026-09-13 수정)

한동안 3×2 격자로 그렸는데 **원본이 자기 위치를 직접 말한다**:

- `r_event_txt[164]` "하단 **첫 번째**에 위치한 [선수정보]"
- `r_event_txt[165]` "하단 **두 번째**에 위치한 [트레이닝]"
- `r_event_txt[249]` "하단 **4번째**에 위치한 [외출]"
- `r_event_txt[197]` "하단 **다섯 번째**에 위치한 [아이템]"

즉 여섯 커맨드가 화면 하단에 한 줄로 늘어선다. 치수도 들어맞는다 —
`slt_frame/000`(채움 상자)이 39px 이라 여섯 칸이 **234px**로 240 화면에 딱 맞고,
`slt_frame/001`(노란 테두리)은 42px 이라 고른 칸에 3px 넘치게 얹힌다.

### 아이콘 번호도 추측이 아니다

`ui/mode_icon.pzx` 의 **000~005 가 StrHOWTO[11] 의 커맨드 나열 순서와 그대로 같다**:

| 번호 | 그림 | 커맨드 |
|---|---|---|
| 000 | 기록장 | 선수정보 |
| 001 | 아령 | 트레이닝 |
| 002 | 집 | 휴식 |
| 003 | 도시 빌딩 | 외출 |
| 004 | 상점 진열대 | 아이템 |
| 005 | PLAY + 스타디움 | 다음경기 |

그림 뜻과 순서가 둘 다 맞는다. 이전에는 015·012·024·021 처럼 번호를 눈대중으로
골라 썼고 그래서 엉뚱한 그림이 나왔다.

**아직 확인 못 한 것**: 훈련 메뉴 아이콘은 `mode_icon/007~` 을 순서대로 쓰고 있는데
근거가 없다. `trainning.pzx` 는 메뉴 아이콘이 아니라 훈련 미니게임 UI(게이지·COMBO·달성)였다.

## 투구 타이밍은 원본 pitch.zt1 에 있다 (2026-09-13 수정)

한동안 비행 시간을 360~600ms 로 **지어내 쓰고** 있었다. 원본에 실제 값이 있다.

`pitch.zt1` 레코드 구조를 다시 읽었다:

```
항목 = u8 구질번호 | u8 레코드수 | 레코드…
레코드 = u16 크기 | u8 단계 | u16 점정보 | (i32 x,y,z) × 점개수
```

- **`단계` 가 공이 날아가는 프레임 수다** (12~35).
- 레코드는 **4개씩 한 묶음이 구속 단계**다: FASTBALL 18/16/14/12, CHANGEUP 24/23/22/21.
- `점정보`는 **하위 바이트만 점 개수**이고 상위 바이트는 변형 번호다
  (514 = 0x0202 → 점 2개). 마스킹을 안 해서 점이 수백 개로 잡히고 있었다.
- `S.CURVE`(24/25/26/28)와 `SPECIAL`(26/29/32/35)은 원본이 **반대로 느려진다**.
  데이터를 그대로 따른다.

### 구질 21종이 5종으로 줄어 있었다

`selectPitch` 가 원본 `PITCH_TYPES` 를 안 쓰고 직구·슬라이더·커브·포크·체인지업
**5개짜리 자체 프로필**을 쓰고 있었고, `pitchCommand` 에는 원본 21개 이름을 그 5개로
깎아내리는 `mapToLegacyType` 까지 있었다. 둘 다 걷어내고 원본 21종을 그대로 쓴다.

### 프레임 속도는 한 값만 우리가 정했다

원본은 시간을 전부 **프레임 수**로 갖고 있고 한 프레임의 밀리초는 binary.mod 안이라
아직 못 읽었다. `shared/config/frameRate.ts` 의 `MILLISECONDS_PER_FRAME = 66`
(≈15fps) 하나만 우리가 정했고 구질·동작 사이의 상대 속도는 전부 원본 그대로다.
전체가 빠르거나 느리면 이 한 줄만 고치면 된다. 타자 스윙 애니메이션도 같은 값을 쓴다.

## 타이틀·메인 메뉴 (2026-09-13 다시 만듦)

한동안 타이틀 그림 위에 모드 글자를 겹치고 소프트키로 이어하기/새로하기를 붙였다 — 원작에 없는 구성이었다.

### 타이틀 — ui/main_title.pzx 프레임 테이블 좌표 그대로

| 파트 | 내용 | 좌표 (근거 프레임) |
|---|---|---|
| 000 | 배경 240×320 (구멍 둘) | (0,0) f15~ |
| 002 | 2010 프로야구 로고 | x −86→−47→−9→4, y 144 (f13~f16) |
| 003 | 뛰어오르는 선수 | y 159→77 (f00·f01), 안착 34 는 구멍 위치 |
| 004 | 흙먼지 | (131,226) f02~ |
| 005 | 공 | (211,0)→(180,14)→(162,31)→(158,34) |
| 007 | 전체이용가 | (0,5) 인트로 중에만 |
| 008 | TOUCH SCREEN | (88,254) f29/f30/f31 깜빡임 |
| 006 | ©2009 GAMEVIL v1.0 | f28 단독 프레임 — **위치 추정** |

인트로 키프레임 프레임 수를 더하면 로고가 x=4 에 닿는 것이 정확히 f16 이라 원본 번호와 맞는다.
추정: 선수 안착 시점, 깜빡임 주기(8프레임), 저작권 문구 위치.

### 메인 메뉴 — main_ui 글자 + StrMAINMENU 설명

- 처음 메뉴: 게임시작 · 스페셜 · 도움말 · 환경설정 · 랭킹 · 게임문의 (`main_ui/frames` 000~005)
- 게임시작 → 최근게임 · 일반모드 · 나만의리그 · 시즌모드 · 대전모드 · 홈런더비 · 미션모드
  (StrMAINMENU [6]~[12] 설명 순서)
- 설명 문구는 `StrMAINMENU` 원문, 흰 패널은 `main_ui/003`(149×63), 글자는 원본 폰트 크기(9×11)에 맞춰 9px
- 저장이 있을 때 나만의리그 → `StrMAINMENU[15]` "새로하시겠습니까? 기존 데이터는 사라집니다"
- **추정**: 목록 좌표(코드 안에 있음), 선택 바 `main_ui/002`(125px = 가장 긴 글자 폭)를 글자 밑에 깐 것, 배너 `mode_back/000`

### 모드 선택은 셀렉트박스 (사용자 요청, 2026-09-13)

원작은 글자 목록에서 고르지만, 사용자 요청으로 토스 TDS 방식으로 바꿨다.
`SelectBox` 를 누르면 `BottomSheet` 가 올라와 모드를 고르고 **시작하기** 를 누른다.

- 공통 컴포넌트: `shared/ui/SelectBox`, `shared/ui/BottomSheet` (배럴에서 export)
- 비활성 옵션은 흐리게, 고른 옵션엔 ✓. 키보드는 ↑↓ 이동 · Enter 선택 · Esc 닫기
- 시트는 `SheetHostContext` 로 화면(stage) 요소에 포털을 연다 —
  셀렉트박스가 position:absolute 칸 안에 있으면 그 칸만 덮어 잘리기 때문이다
- 모드 이름·설명·순서와 새로하기 확인 문구는 여전히 StrMAINMENU 원문
- 처음 메뉴(스페셜·도움말·환경설정·랭킹·게임문의)는 기능이 없어 화면에서 뺐다

### 나만의 리그 메뉴 위 배너

관리 화면 맨 위에 `mode_back/000`(240×84 경기장)을 붙였다. 메뉴 위에 올 만한 화면 폭 그림을
전부 뒤졌는데 이것뿐이었다 — `mode_back` 프레임 3장은 픽셀까지 같은 그림이고,
`raise_traning_ani`(깡통·타이어·폭발)와 `team_traning_ani`(작은 조각)는 훈련 미니게임 이펙트였다.
**어느 화면에 쓰였는지는 코드 안이라 확정은 아니다.**

## PZX 정식 구조 (2026-09-13, 서브 에이전트 A 발견 · 직접 바이트 검증)

헤더 `'PZX' ver | u32 이미지구간 | u32 프레임구간 | u32 애니메이션구간(=0x10)`.
구간은 `u8 flag | u16 count | 본문` — flag 맨 아래 비트가 1 이면 zlib.
압축이면 상대 오프셋 × (count+1) + u32 압축크기 + zlib, 비압축이면 절대 오프셋 × count.
프레임 `u8 파트수 | u8 박스수 | 박스×8 | 파트(u16 이미지, i16 x, i16 y, u8 효과수, 효과…)`,
효과는 `u8 종류` + (0x60~0x6F 만 u32). 애니메이션 `u8 개수 | (u16 프레임, u16 지연, 4바이트)`.
파트는 파일 순서대로 겹친다(앞이 아래).

한동안 파트 수를 u16 으로 읽고 뒷바이트를 역추적으로 맞췄다 — 헬멧이 반쪽만 나오거나
겹침이 뒤집히고, **애니메이션 표 78개와 훈련 선수 프레임을 통째로 못 읽었다**.
142개 파일 전부 남는 바이트 0으로 풀린다. `frames/animations.json` 을 새로 낸다.

## 훈련 팝업 (2026-09-13, 서브 에이전트 B · binary.mod 역분석)

`binary.mod` 는 ARM ELF, `.text` VA 0x1000 (VA = 파일오프셋 + 0xfcc), Thumb 코드.
0x84664(popup, 메뉴칸) 가 연출을 고른다. 타자편(모드 4, 표 0xd497c):

| 칸 | 훈련 | 연출 | 캐릭터 |
|---|---|---|---|
| 0 | 히트 | raise_traning_ani 1 | 동작표 0xd49e8, 가운데+0x34, 바닥−10 |
| 1 | 파워 | raise_traning_ani 0 | 동작표 0xd4a48, 가운데+0x37, 바닥−2 |
| 2 | 수비 | team_traning_ani 4 (혼자 뜬공 캐치) | 없음 — 프레임 안에 선수 |
| 3 | 주루 | team_traning_ani 3 (혼자 끌며 달리기) | 없음 |
| 4 | 모든능력치 | raise_traning_ani 2 | 칸 0 과 같음 |

- 연출 위치는 고정이고 움직임은 프레임 그림 안에 들어 있다 (애니메이션 표의 dx/dy 는 전부 0)
- 지연 0 은 1 로 올리고, 지연만큼 갱신이 지나야 다음 칸 (0x93d90). 팝업은 반복 재생
- 게이지 0x84634(this, 31, 60) — 60번 갱신하면 가득 차서 닫힌다
- **타자편 메뉴는 다섯 칸**이다. 체력·타격·집중·근성은 투수편·시즌 팀 능력치라 뺐다
- 시즌 모드(표 0xd4968)·투수편(표 0xd4990, 결과 등급 k 로 연출 3종 분기)도 표가 있다

**아직 못 읽은 것**: 팝업 사각형 크기·배경(0x94a65), 연출 세로 보정값, 캐릭터 동작 번호→그림(0x79525),
갱신 한 번의 밀리초, 훈련 비용·사기·상승치(서브 에이전트 C: 데이터 표에 없음 → 코드 안),
`trainning.pzx` 미니게임 규칙(0x36714 등). 코드: `shared/config/original/trainingAnimation.ts`,
`widgets/training-scene`.

## 이벤트 스크립트 r_event 해독 (2026-09-13, 서브 에이전트 D · E 교차 확인)

오랫동안 "Ghidra 급이 필요하다"며 미뤄둔 `r_event.zt1` 을 해석기 코드로 풀었다.
`tools/decode_events.py` — r_event 313개 · s_event 30개 이벤트, **남는 바이트 0**, 대사 번호 전부 범위 안.

- 레코드: `u16 길이 | u16 id | u8 enabled | u8 repeatable | u8 trigger | u16 requiresEvent | 날짜 창 4바이트 | u8 byte13 | 조건 × (u8 종류, s16 값) | 명령`
- 명령 실행기 0x8cf64: 0 대사 · 1 선택지(분기 이벤트) · 2 시스템(0=알림) · 3 예/아니오 · 5 화면효과 · 6 소리 · 7 보상 · 8 경기
- 화자: 0 없음 · 1 플레이어 · 2~24 = `StrMODE[번호+91]`
- **초상화**: 4바이트 `[캐릭터, 좌우, 미사용, 표정]`
  - 파일: 캐릭터 10~13 → event_char_1, 17~22 → event_char_2, 나머지 → event_char_0
  - 애니메이션 = 기본번호표(VA 0xd0ae6, 파일 0xcfb1e) + 표정 — 에이전트 E 의 조회표와 같고, 코드가 가리키는 주소를 직접 검색해 확인했다
  - 자리: 좌우 각 3칸, 목표 거리 45·75·105, 한 번 갱신에 거리의 1/6 씩 미끄러져 들어온다 (0x7f998)
  - 대사·선택지마다 초상화 묶음을 통째로 바꾼다
- 인물 그림은 새 디코더로 뽑으면 **얼굴까지 완전하다** (예전 디코더가 파트를 빠뜨렸던 것)

웹판: `shared/config/original/events.ts`(지연 로딩) · `eventMeta.ts` · `entities/story/model/eventScript.ts` ·
`widgets/event-portraits` · `pages/story`. 추정으로 나눴던 `story.ts`(152장면)와 "선택지 보상 60G"는 지웠다.

**아직**: 이벤트를 띄우는 조건 일부(트리거 종류 18~25, 호출 지점) — 지금은 원본 번호 순서로 하나씩 보여준다.
보상 종류 0~21 의미(0xd4e50), 화면효과 이름, 초상화 세로 위치.

## PZX 애니메이션 한 칸 (에이전트 E 가 A·B 의 오독을 잡음, 직접 확인)

`u16 프레임 | u8 지연 | i16 dx | i16 dy | u8 플래그`. u16 지연으로 읽으면 6404 같은 값이 나오고,
이렇게 읽어야 엔딩 그림이 dx 0→53 으로 흐른다. 훈련 파일은 dx/dy 가 0 이라 우연히 같았다.

**디코더 추가 수정 (에이전트 E 지적 → 바이트로 확인 후 반영)**
- 이미지 구간 헤더 `u8 8 | u16 이미지수 | u8 flag | [bit0: u8 색수(0=256) | 색…] | u32 해제 | u32 압축 | zlib` 로
  공용 팔레트를 읽는다 (마젠타 찾기 폐기 — 색 둘을 더 읽었다)
- 인라인 팔레트는 첫 색이 마젠타가 아닐 수 있다 → 뒤따르는 헤더 표식 `02 CD CD CD` 로 확인.
  mode_ui 15장·game_ui 14장이 0×0 으로 빠지던 것 해결
- 0번 색은 마젠타일 때만 투명
- 효과: 0x04 상하 뒤집기 · 0x66 n 반투명 n/16 · 0x67 n 반투명 n/255 · 0x6E/0x6F 단색 채우기 (뜻은 추정)
- **아직**: 0x05~0x0C `.mpl` 대체 팔레트 · 0x7E 뜻 · 박스(히트박스) 버림

## 훈련 팝업 추가 확인 (에이전트 B 2차)

- 창: `ui/mode_ui.pzx` 프레임 10 박스0 = (0, 65, 240, 72) → 연출 위치 **(120, 137)**
- 배경: `mode_back` 프레임을 시각으로 고른다 (6~15시 0 · 16~19시 1 · 그 밖 2), (1, 52) 에 그리고 잘라낸다
- 캐릭터: 우리 육성 선수 레이어 — 몸통 batter_balancer/sluger(타입), 헬멧, 배트 batter_batter,
  장비 item_bat_*, 순서표 0xd3a54 로 겹침 순서가 바뀐다. 그림자는 팝업에서 끈다
- 게이지: mode_ui 프레임 58 (1,137) + 프레임 59 를 1px 씩 60칸
- 갱신 주기: 속도 옵션 표 4·10·16·22·28fps → 250·100·62·45·35ms (옵션 기본값은 못 찾음)
- **웹판 반영 (2026-09-13)**: `widgets/training-scene` — 창 잘라내기·연출 (120,137)·게이지 프레임 58/59·
  캐릭터 레이어(몸통·헬멧·배트·몸통앞 +14, 순서표 0xd3a54). 한 번 갱신은 옵션 표 가운데 62ms(기본 옵션값 미확인)

## 원본 대비 감사 후 수정 (2026-09-17)

서브에이전트 4개로 원본(추출 자료·설명서)과 웹판을 영역별로 대조하고 고친 것.

**미션 레코드 숫자 열 해독** — `generate_missions()` 가 이제 고정 위치로 읽는다:
```
u8 id | u8 단계 | u8 ×6 | u8 (윗4비트 = 상대 마선수 순번) | u16 제한 | u16 제한시간 | u8 조건코드
| 목표 32 | 이름 32 | 설명문 64 | ...
제한 u16: 상위 바이트 = 타석, 하위 바이트 윗4비트 = 스윙, 아래4비트 = 투구 수
```
- 제한시간을 u8 로 읽어 노히트노런·퍼펙트가 300초 → **44초**로 나왔었다
- 조건코드 바이트가 목표 문자열 앞에 붙어 `'\x01아웃'` 이 되어 **투수 미션 5개가 클리어 불가**였다 (조건코드 1~3 뜻은 미해독)
- 마선수 순번은 원본 마선수 표 순서와 일치 (타자 미션 4=레오니, 투수 미션 12=킹타이거) → 미션 상대 능력치·그림에 반영
- 설명문 정규식 대신 레코드 값을 쓴다. 설명문은 목표 **개수**에만 쓴다 ("6구", "모두" 규칙 포함)
- 사이클링히트(단타·2루타·3루타·홈런), 만루홈런(=만루 미션의 홈런, 4타점), 무실점·무안타 조건(깨지면 실패) 판정 추가
- **그라운드홈런은 추정**: 타구 판정에 장내 홈런이 없어 3루타로 센다
- `missionClearability.test.ts` — 38개 미션을 이상적인 결과로 끝까지 돌려 전부 클리어되는지 검사

**미션 모드 진입 불가 수정** — 커리어가 없으면 진입 화면으로 되돌려 미션 화면에 닿지 못했다.
미션은 육성 선수가 없으면 신인 능력치로 한다. 관리 화면에 `메뉴` 소프트키(메인 메뉴로) 추가.
클리어 기록은 `편:번호` 키로 localStorage(`compus-baseball/mission-cleared`)에 저장 — 예전엔 번호만 써서
타자 12번을 깨면 투수 12번도 깬 것으로 보였고, 새로고침하면 사라졌다. 투수 미션도 제한시간이 흐른다.

**이벤트** — 보상(명령 7)을 적용한다. 뜻이 앞뒤 대사로 분명한 종류만: 0 인기도 · 1 평판 · 2 사기 ·
10 소지금(만원) · 11 부상 (`entities/story/model/eventReward.ts`). 19 타순, 6 마구, 13~16 등은 미적용.
선택지·예아니오·경기 결과로만 가는 **분기 이벤트 63개**는 순서 재생에서 건너뛴다 (단독으로 튀어나오던 문제).

**외출 오독 정정** — `StrHOWTO[17]~[22]` 는 투수편이 아니라 **시즌모드** 구간이다. 친선경기·회식·구단CF 제거.
`StrITEM[148~154]` 는 서브아이템 88~97 의 추가 효과이지 장소 기본 효과가 아니다. 장소 수치는 우리가 정한 값.
휴식은 부상·질병도 낫게 한다 (StrHOWTO[14]). 시즌은 원문대로 **45경기** (StrHOWTO[10]).

**랄프 루프 1회차 추가 (2026-09-17)**
- 도움말 화면 — StrHOWTO 35쪽 원문 (`pages/help`, 메인 메뉴 우상단). 묶음 경계는 "<…에 대하여>" 제목 기준(추정).
  마크업 `!L`(왼쪽 정렬) 해석 추가
- 관리 주기 규칙 — 트레이닝·휴식·외출은 주기마다 하나 (r_event_txt[176]), 트레이닝은 사기 감소 (r_event_txt[173], 양 5 는 추정).
  `PlayerCareer.hasActedThisCycle`, 경기를 치르면 초기화. 외출 [!] 이벤트도 행동으로 친다
- 로딩 화면 — ui/loadingbar.pzx 상자 + StrTIP 73개 중 하나, 달리는 선수(애니메이션 0). 경기 시작 전에 띄운다.
  상자 위치(가운데)·선수 경로·길이 30갱신·띄우는 시점은 추정
- 서브에이전트 상시 운용: position-re(타석 좌표 역분석) · gap-finder(누락 탐색) · checker(변경 검증)

**랄프 루프 2회차 (2026-09-17)**
- 미션 목표 개수를 레코드 뒤쪽 칸에서 읽는다 (점검 에이전트 발견, 38개 전부 설명문과 일치):
  타자 160=단타/2루타 · 161=3루타 · 162=그라운드홈런/만루 · 163=홈런 · 164=도루/번트 · 165=타점 · 166=안타,
  투수 160=삼진콤보/탈삼진 · 161(위)=MAX게이지 · 164=아웃. **투수 1번은 아웃 3개** (설명문엔 숫자가 없어 1개로 잘못 읽었었다).
  설명문 정규식 해석은 이닝 수("5이닝")만 남겼다. 노히트노런·퍼펙트 막대는 이닝×3 아웃 기준
- 미션 목록은 **28개** (StrHOWTO[27]) — 단계 0 "OO 공략" 10개는 마선수 대결용 레코드라 목록에서 뺐다
- 이벤트 보상 11 = **질병** (490 "몸이 으슬으슬"). 보상 상한은 점프 표 0xd4e50 — 사기 100·평판 999·인기도 9999
- 순서 재생은 enabled=0 이벤트(분기·평가·타순·엔딩)를 건너뛴다. 첫 칸 오프닝(451)만 예외 (추정: 코드가 직접 부름).
  `eventMeta.LAST_SEQUENTIAL_EVENT_INDEX`(311) 로 [!] 표시를 끈다
- 칭호: StrNICKNAME = 이름 64 + 조건 원문 64. 명성 사다리(지어낸 규칙)를 버리고 원문 조건 23개를 판정
  (훈련·외출 횟수, 평판·인기도, 소지금, 통산 기록, 한 경기 4홈런, 사이클링 2회, 6년차 4할, 9·13년차).
  MVP·우승·공략·복권·도루·투수 기록 조건은 기록이 없어 아직 판정하지 않는다
- 상한: 평판 999 · 인기도 9999 (칭호 조건 + 보상 점프 표)
- 리그 상대는 기본 10팀 (StrHOWTO[7], 히든 5팀 제외)
- 환경설정 화면: 속도 5단계(표 0xd7624, `millisecondsPerFrame()` 으로 매번 읽음) · 투구 기본/게이지 ·
  나만의리그 초기화(StrMAINMENU[210]). 기본 속도 62ms·투구 기본값 게이지는 추정. 사운드·진동·주루·송구·전광판은 아직
- 스킬 40종(StrCOMMON[55~94] 이름 · StrSKILL[0~39] 소개 · [40~79] 효과)은 데이터는 있으나 **획득 조건 미해독**이라 보류
- 서브에이전트 layout-re 추가 (화면별 원본 그림·합성 좌표 검증 — 사용자 요청 "메뉴만 있는 화면은 없을 것")

- 타석 화면을 원본 좌표로 옮김 (위 "타석 배경 배치" 표). 경기 배너는 원본 기록달성 이름 — 3루타, 솔로/2점/3점/만루 홈런 (StrGAME[8]~[12])
- 부상·질병: 부상 능력치 −60% (StrMODE[212]), 질병 −30% (r_event_txt[537]), 질병 이름 4종, 사기 0 훈련 불가·사기 최고 휴식 거절·건강 시 입원 거절.
  경기 뒤 사기에 따라 발생(시점·확률·부상/질병 반반은 추정)
- 경기 중 [메뉴] → StrGAME[0] 원문 확인 → 메인 메뉴 (이 경기 기록은 사라짐)
- 경기 결과 화면에 ui/result.pzx "YOU WIN"/"YOU LOSE" 그림 (위치 추정, 무승부 그림은 원본에 없음)
- 마크업 !C/!L 은 다음 전환까지 유지 (StrHOWTO[32] 근거), 행동권은 관리 주기가 열릴 때만 복구(이어하기 악용 방지), [!] 이벤트도 외출 횟수에 셈

**랄프 루프 3회차 (2026-09-17)**
- **외출 지도를 원본 프레임 배치로** (layout-re): event_map 프레임 1~5 = 경기장(64,119)·번화가(127,77)·병원(0,84)·학교(179,143)·방송국(78,209),
  박스 0 이름칸(img_text 229~233) · 1 선택 화살표(애니 0) · 2 [!](애니 2, 프레임 11·12) · 3 말풍선. 프레임 6 = 길 점선.
  240×320 화면에 지도를 세로 가운데(y=11) — 이전 좌표는 전부 틀렸었다. 장소 기능 목록 틀(말풍선)은 아직
- **이벤트 일정을 원본 레코드대로**: trigger 0 관리·1 외출 진입·2~6 장소(프레임+1, 대사 인물로 확인),
  audience 0 코드호출·1 공통·2 타자·3 투수(점검 에이전트), 날짜 창 = [연차, 경기 번호], requiresEvent, 조건 18(인기도, 추정)·22(질병 확률표 0xadb32).
  조건 19·20·21·25 는 판정식 미해독이라 그런 이벤트는 아직 안 띄움 (20/21 = 스킬 번호+1 획득/해제 조건으로 보임).
  본 이벤트는 seenEventIds. 오프닝 451 → 이벤트 1(1년차 1경기) 흐름을 브라우저로 확인. `storySceneIndex` 는 더 이상 안 씀
- 질병은 이벤트 490 으로만(보상 11: 값≥0 무작위 질병 이름, 음수 치료). 경기 뒤 판정은 부상만(추정)
- 이벤트 대사의 팀 이름은 소속 팀(TEAMS) — 예전엔 "캠퍼스" 고정
- 스킬 40종 구조화 데이터 (`original/skills.ts`: 이름·소개·효과·대상). 획득은 이벤트 조건 20/21 + 보상 4(±스킬번호+1)로 보이나 조건식 미해독
- **이벤트 조건 추가** (점검 4차, switch 0xd83a0): 0~3 능력치(×10, 순서 추정) · 19 평판 · 24 봤음 · 25 안 봤음. 반복 이벤트도 보면 막히고
  저장을 불러올 때 풀림(0xacf60). 질병 쿨다운 20(+0x7c, 경기당 1 감소는 추정)
- **보상 종류 정정** (누락 탐색 2차, 0xd4e50): 3 = 소지금 100만원 단위(상한 9999) · 10 = G포인트(상한 99999) · 20 = 연봉 변동
- **시즌 흐름** (`entities/career/model/seasonFlow.ts`, `app/model/seasonEvents.ts`):
  올해의 목표 표(binary 0xd7f9e → `original/yearGoals.ts`, 타입별 2표 중 첫 표 사용은 추정) · 22경기 뒤 중간평가 452~454 ·
  45경기 뒤 시즌 성적 → 392 → 393~396 → 연말(방출 501·13년차 504·7년차+ 은퇴 선택 502·연봉 380) ·
  연봉 강경/정중 → 결과 384~391(타이틀 등급 0 고정) → 새 시즌(연봉을 소지금에, 단위 추정) ·
  엔딩 판정표(0xa3a84) → StrENDING[0~9] 엔딩 화면 → 선수 삭제(명예의 전당 없음)
  **아직 없음**: 포스트시즌·한국시리즈, 개인 타이틀·MVP(370~377), 국가대표(461~464, 격년), 부상 누적 엔딩, 전설(9)·연애 엔딩(10~14),
  타순 이벤트(470~489, 9번 시작), 감독 평가 글(USER_EVT 39~74), 외출 빈 장소(440~444)
- **타순** (`entities/career/model/battingOrder.ts`, 0xa4c2c): 9번 시작, 경로 [9,8,7,6,5,3,4]/[9,8,7,6,2,3,1](487→488/489, 보상 18),
  평판 승격 200~900 → 470~478, 강등 150~825 → 479~486, 보상 19 = 새 타순. 경기 뒤 관리 화면에서 먼저 확인. 경기도 이 타순으로 선다
- **미션 시작 상황·실패 한도** (점검 3차, 0xaae7c·0xaac76): 레코드 3번(시작 이닝)·4번(아웃/볼/스트라이크, 볼·스트라이크 순서 추정)·
  5번(주자 4=1루·2=2루·1=3루) → `missions.ts` start. 타자 미션은 주자 진루 규칙으로 타점을 센다(만루홈런은 만루일 때만),
  도루는 1루 주자가 있을 때만·실패하면 주자 아웃. 3아웃이면 시작 상황으로 되돌림(추정).
  투수 실패 한도 161 아래=실점·162 위=볼넷·162 아래=피안타 → failLimits, 닿으면 실패(무실점·무안타·무사사구).
  이닝 목표 = (9 − 시작 이닝 + 1) × 3 아웃 — 설명문 해석을 모두 걷어냈다. 첫 타석은 레코드 볼카운트로 시작
- **외출 빈 장소**: [!] 없는 장소에 들어가면 440~444 "특별한 일이 없다" (0x16ccc) — 행동을 쓴다(추정)
- HUD 를 원본 배치로 (`widgets/batting-stage/lib/renderHud.ts`, 0x373d0 + game_ui 프레임 0 박스). 숫자 글꼴 번호는 추정
- 미해독·다음 작업 (position-re 2차): 공 궤적 원본 식(베지어 + 휘기 보정 + 27° 투영, 존 중심 표 0xcfbcc), 커서 없음(타자 좌우 ±3, 범위 ±9),
  HUD = game_ui 프레임 0 박스 배치 (6,6), 하늘 색 쌍 표 0xd37f2. 미션 시작 상황(레코드 3·4·5번 바이트 = 이닝/카운트/주자), 투수 실패 한도 칸(161 아래 실점·162 볼넷/피안타)

**랄프 루프 5회차 (2026-09-17)**
- 하늘 색 원본(0x77fe8 기본 쌍 (64,137,190)→흰색, y 10~118 그라데이션). 구름 위치·펜스는 미해독
- HUD 점수 숫자 프레임 20~·자간 −3·아래 줄 y+3, 공격 막대 h−2 (점검 5차)
- 연봉 반올림 원본식(base ± trunc), 상한 65535 · 목표 타율은 버림 · 엔딩 인기도 정확히 1000 은 판정 없음
- 칭호 1 "떠오르는 샛별"(1년차 중간평가 5개) · 9 "제 2의 전성기"(지난 중간평가 ≤1 → 5개), `lastMidSeasonGoalCount`
- **보상 4 = 스킬** (+n 획득 n−1, −n 해제) → `PlayerCareer.skillIds`. 엔딩 9 전설 = 13년차·인기도 >3500·스킬 7
- **경기 후 평가** (`entities/career/model/gameEvaluation.ts`, 누락 탐색 3차): 타석 점수(0xa59c0) → 인기도 변화 −2~6(0xa690c) →
  평판 변화 앞부분(0xa6218) → 사기 승 +5/패 −7(행운 +1, 무승부 0 은 추정) → 감독 평가 글 USER_EVT 39~74(평판 구간 × 등급, 타순 3번 이내 2배 표) →
  결과 화면에 원문 글과 USER_EVT[75] 변화량. 연속 기록(2안타·홈런·무안타 경기) 코멘트 108/110~112·평판 보상·하락세 스킬 17 획득/해제
- **외출 기능 원본 표** (점검 6차, 0x16cf0): 소지금 팬미팅 −500만·외식 −100만·입원 −200만·CF +800만, 필요 인기도 600/0/0/200/400,
  외식은 사기 100 이면 막힘. 사기·인기도·평판 변화량은 아직 추정
- 타순 경로 미선택은 4번 경로 기본(원본 +0x57=0) · 미션 화면에 HUD(시작 이닝·아웃·주자·카운트, 점수판 142~159 합)
- **스페셜 메뉴(오프라인 부분)** — 메인 메뉴 우상단 [스페셜]: 기록연감(닉네임 64·스킬 40·엔딩 15, 못 얻은 것은 ???) ·
  명예의 전당(타자 4칸). 엔딩 뒤 StrMODE[215] 로 등록을 묻고 StrCOMMON[51]/[52]·StrMODE[219] 원문으로 답한다.
  게임 전체 기록은 `compus-baseball/collection` (공용 JSON 저장소 `localStorageJsonStore`). 플레이 시간 기록·슬롯 구매·
  마선수 선택·에디트·통신 기능은 없음. "나중에 등록" 은 원본과 달리 선수를 남기지 않는다(저장이 하나라서)
- **랄프 6회차 (점검 7·8차, 누락 탐색 4차 반영)**
  - 경기 후 평가 식 정정 (0xa59c0·0xa6218·0xa73c4·0x1283c):
    - 타점 안타는 표 값만 준다(타점 수를 더하지 않음).
    - −1 은 병살(한 플레이 2아웃)일 때만. 타점 없는 안타의 진루 보너스(r6)는 뜻을 몰라 뺐다.
    - 평판: 무안타 −5, 병살 2개 이상 −2, 득점권 아웃 −1.
    - 사기: 승리가 아니면 모두 −7(무승부 포함). 라이벌전(0,1)(2,7)(3,6)(4,5)(8,9)은 ×2.
      인기도 변화가 2 이상이면 절반만큼 덜 잃는다. 세 보정의 적용 순서는 추정이다.
    - 감독 평가의 타순 2배 표는 투수편 전용이라 타자는 쓰지 않는다.
    - 연속 기록 알림 앞에 경기 수를 붙인다 ("3경기 연속 무안타").
  - 엔딩 뒤 흐름 (0x1220c):
    - 부상·방출(0·1) 엔딩: StrMODE[221] "5000 G포인트로 이어하기". 모자라면 StrCOMMON[41].
      이어한 뒤 다음 시즌으로 가는 것은 추정이다.
    - 엔딩 2 이상: 엔딩 보너스 표 0xcc40c(×1000 G, 바이트 직접 확인)와 StrMODE[214] → 215 등록 질문.
      219 "나중에"는 예/아니오 질문이고, 아니오면 등록 질문으로 돌아간다.
    - 원본 G포인트는 전역 값이라 선수를 지워도 남는다. 웹판은 선수 안에 있어 보너스가 함께 사라진다 — 차이로 남김.
  - 신인 초기값 원본화 (0x11244): 인기도 0 · 평판 300 · 사기 100 · 소지금 6000만 · 연봉 50.
    연봉의 표시 단위는 미확인이다.
  - 스페셜 화면의 디버그 문구 "StrMAINMENU[22]" 제거.
    엔딩 목록은 "%s은(는)" 같은 이름·조사 묶음을 빼고 요약한다.
    명예의 전당 저장 항목은 형식을 검사하고, 깨진 항목만 버린다.
  - 미션 화면 가로 스크롤 제거: 캔버스 여백 −10px, 본문 스크롤바 숨김.
  - **다음 핵심 과제 — 능력치 척도**: 웹판은 0~100, 원본은 0~999다.
    원본 훈련 상승 4~7, 타격 판정 0xab214, 이벤트 조건이 모두 999 척도라
    훈련(누락 4차 TOP 2)과 타격 판정 이식 전에 척도를 옮겨야 한다.
- **랄프 7회차 (점검 9차, 누락 탐색 5차, layout-re 관리 화면 명세)**
  - **능력치를 원본 눈금 0~999 로 옮겼다.**
    - 이전 판정 엔진(judgeSwing 등)은 ABILITY_SCALE(1000)로 나눠 비율을 쓴다.
    - 투구 엔진은 아직 0~100 이라 pitcherAbilityOf 경계에서 10으로 나눈다.
    - 세이브는 형식 2로 올렸다. 형식 1(0~100)은 불러올 때 10배로 옮긴다.
  - **능력치 순서 정정 [B]:** 레코드 +0xc 는 히트·파워·**수비·주루** 순이다.
    근거는 훈련 칸 i → 인덱스 i, 스킬 20 수비 −100, 필살수비가 인덱스 2 를 읽는 것이다.
    예전 생성기는 수비와 주루를 바꿔 읽었다. 투수는 제구·구속·변화·체력 순이다.
  - **신인 시작 능력치** 표 0xcc3fa:
    - 타입 0 → 100/100/100/100, 타입 1 → 80/150/80/80.
    - 둘째 선택이 0 이면 수비 +30, 아니면 주루 +30. 생성 화면이 아직 없어 (0, 0) 으로 시작한다.
    - 신인 스킬은 0 병아리와 8 의외성이다.
  - **훈련 원본화 (0x17f5c):**
    - 칸 0~3: 상승 4~7(수비·주루 5~8), 타입 보너스 +1, 사기 −5~8, 비용 없음.
      사기 0 이거나 능력치가 999 면 막힌다.
    - **칸 4 는 필살타법 레벨 훈련이다** ("모든능력치" 가 아님).
      필요 횟수 4/5/6/7, G포인트 500/700/900/1200, 사기 −9~13(병아리 −1, 몹쓸몸 +2), StrMODE[86]/[87].
    - 타입별 한계치(800 계열)는 GP 아이템에만 확인됐고, 훈련에는 아직 적용하지 않았다.
    - 서브 아이템 보너스(+2, 사기 −1)는 서브 아이템이 없어 빠져 있다.
  - **점검 9차 반영:**
    - 사기 순서: 라이벌 ×2 → 인기도 절반 보정 → 행운.
    - 새 시즌: 사기 100, 소지금 += 연봉×100만, 상한 9999×100만.
    - 연봉은 원본 100만 단위 그대로 둔다.
    - 이어하기: 부상 엔딩은 같은 시즌에서 부상만 푼다. 방출 엔딩은 새 시즌 전환이다.
    - 득점권 아웃은 2루 주자만 본다. 병살·득점권 카운터는 타점도 끝내기도 없는 타석에서만 센다(atBatPenaltyCounts).
      주자를 플레이 전 기준으로 보는 것은 추정이다.
  - **관리 화면을 원본 240×320 배치로 다시 만들었다** (0x7d34c·0x7e418, `pages/management/lib/managementLayout.ts`).
    - 구성:
      - 계단형 판, 경기장 띠(시각별 프레임), 이름·칭호 띠, 사기 게이지(빨강/주황/초록)
      - 인기도·소지금·평판·연봉 값 칸(num 숫자, "억")
      - 메시지줄 "N년 G/45경기", 상태 아이콘
      - 계단형 커맨드 6칸(선택 칸만 주황 팔레트와 테두리 이름표, 비활성은 흑백)
    - 팔레트 변형 그림은 `tools/generate_management_sprites.py` 가 `public/sprites/management/` 에 만든다.
    - layout-re 합성 그림과 픽셀 대조한 결과, 차이는 경기장 시각 프레임·시스템 글꼴·상태 아이콘뿐이다.
      값 막대 오른쪽 끝 4열은 원본 효과 3(좌우 뒤집기)을 명세 합성이 빠뜨린 것이라 웹이 맞다.
    - 원본에 없는 것은 왼쪽 위 "‹ 메뉴" 버튼(휴대폰 종료 키 대신) 하나다.
    - 빠진 것: 등장 애니메이션(사인표 0x6c6a8), 메시지 팝업 원본 모양(0x7f4ec), 칸 0 의 mode_ui 60 괄호.
    - 칭호는 마지막으로 얻은 칭호를 보여 준다(추정).
  - `FrameSprite` 를 shared/ui 로 옮겼다 (외출 지도·관리 화면 공용).
  - 이벤트 보상 종류 13~16 = 능력치 히트·파워·수비·주루 +값, 상한 999 (0x8c758). 원본 이벤트에서 23번 쓰인다.
  - 실효 능력치에 스킬 보정을 넣었다 (0xb6414): 무력감(5) −100, 전설(7) +50, 스킬 20 은 수비만 −100.
    컨디션 표(60~100%)의 등급 출처와 장비 레벨 보너스는 아직 넣지 않았다.
- **랄프 8회차 — 타석 판정 원본 이식 (position-re 3·4차, 포크 작업)**
  - **판정 흐름**:
    - 타이밍: 스윙 순간 공 프레임 F 로 계산하고 F = N−2 가 100 이다 (0x34be0).
    - 스윙 결과 0xab214 (`entities/batting/model/swingResult.ts`·`swingSkills.ts`):
      - 조준 오차 6구간, 제구 등급 배율, 나만의리그 보정, 같은 구질 +500, 스킬 보정을 옮겼다.
      - 난수 순서를 지킨다. 번트 난수를 먼저 뽑는다.
      - position-re 기대값과 정확히 같다 (timing 100/79/54 → contact 12000/9480/6480, B 4818/3432/1782, C 1241/884/459).
    - 타구 방향 0x5141c, 제구 등급 0xb74bc, 구속 단계와 비행 틱 수 N (0x34968·0x66c98).
    - `pattern.dat`/`pattern_plag.dat` 를 생성기가 `battedBallPatterns.ts` 로 옮긴다. 0xb0614 규칙으로 섞어 차례로 꺼낸다.
    - 공 궤적은 원본 정수 베지어(0xbcb84·0xbcc6c)로 한 틱에 한 점, N 점을 쓴다.
  - **조작 (0x53670)**:
    - 커서 조준을 없앴다. 좌우(←→/4·6)는 타자 3px 이동, ±9 까지다.
    - 스윙은 Space·Enter·5. 번트는 8/7/9 로 종류 1/2/3 을 켜고, 다시 누르면 끈다.
    - 웹 전용: Shift = 번트 1, 터치는 좌우 가장자리 탭 = 이동, 길게 누름 = 번트.
  - 이전 `judgeSwing`·`resolveBattedBall` 은 지웠다. 투수편 CPU 타자도 같은 원본 판정을 쓴다.
  - **추정 (코드 주석 표기)**:
    - 안타/아웃은 pattern 분포에 따른 position-re 근사다. 원본은 수비 시뮬레이션이고 미해독이다.
    - 궤적 2D 제어점과 릴리스점 (86,164), 존 1.0 = 16.5px.
    - 번트는 홈플레이트 도달 시 판정한다. 타자 이동은 타석 사이에 초기화한다.
    - 스킬 16·17 은 보유만 하면 적용한다. 좌우 side 는 둘 다 0 이다.
    - 내 선수 레벨은 0 이다. CPU 구질·코스는 여전히 무작위다(투구 AI 0x9eeac 미이식).
  - **남은 차이**: 투구 AI(pitchpattern.arr), N 예외(레벨 7·9), 스윙·번트 애니 표, 뜻 미상 보정 두 개, 필살타법 보너스, 홈런더비, 투수편 입력.
- **랄프 8회차 — 서브 아이템 10종** (`entities/career/model/subItems.ts`, 누락 탐색 5차):
  - 이름은 StrITEM[88~97], 가격은 표 0xcc430 × 1000만원(1억~2.5억, 바이트 확인). 영구 보유다.
  - 효과:
    - 표적판·1톤 바벨·모래주머니·하드타이어: 해당 훈련 +2.
    - 자동안마기: 훈련 사기 감소 −1.
    - 나머지 5종 외출 보정(팬미팅 인기도 +2, 외식 사기 +4, 입원 무료·사기 +1, 야구교실 인기도·평판 +1, CF +400만)은 StrITEM[150~154] 문구대로 넣었다.
      코드에서 적용되는 곳은 미확인이라 추정이다.
  - 상점에 서브 아이템 목록을 붙였다. 알림은 StrMODE[77]·[78] 이다.
    기존 G포인트 아이템 4종은 여전히 원본에 없는 것이다 — 누락 탐색 6차에서 원본 상점 구조를 받는 대로 바꾼다.
- **랄프 9회차 — GP 아이템 효과** (`entities/career/model/gpItems.ts`, 0xa4488, 누락 탐색 5차):
  - 타자편 10종: StrITEM[98~106]·[108].
  - 능력치 +10 과 모든 능력치 +10 은 타입 한계치 표 0xd80c6 × 10 을 넘지 않는다 (타입 0: 800×4, 타입 1: 800/850/750/750, 바이트 확인).
  - 영지버섯 사기 +40, 종합건강진단 치료, 최면요법 마이너스 스킬 삭제, 이글아이 +20경기(상한 99).
  - 또또상품권:
    - 누적 확률 2/5/10/20/40/70/71/75/85/100% (0xd80b1).
    - 1~6등 상금 100/50/20/10/5/2 × 100만 (0xd80a7).
    - 엄마상·메디카상·우정상·아차상은 각각 도시락·건강진단·영지버섯·무작위 능력치 아이템 효과다.
    - 구매 수(상한 200)와 1등 횟수를 센다 → 칭호 21 "행운의 사나이"·22 "도박묵시록".
  - 추정: 한계치를 이미 넘은 값은 줄이지 않는다.
    마이너스 스킬 목록(원본 0x5f350 비트1)은 효과 문구로 골랐다: 2·3·4·5·17·18·19·20.
  - GP 가격 표를 아직 못 찾아서 상점에는 연결하지 않았다.
- **랄프 10회차 (점검 10차, 누락 탐색 6차)**
  - **원본 난수는 [a, b) 다** (bfa55 = a + rand mod (b−a), 점검 10차).
    훈련 상승 4~6(수비·주루 5~7), 사기 −5~7, 필살타법 사기 −9~12 로 바로잡았다.
    원본 식을 옮긴 곳은 `shared/lib/random/originalRandom.randomIntegerBelow` 를 쓴다.
  - **타입 한계치** (`entities/career/model/abilityLimit.ts`, 표 0xcc502 = 0xd80c6 = 0xd4d90):
    - 훈련은 기본 능력치가 한계 이상이면 거절한다 (StrMODE[192]). 실제 상승은 999 로만 자른다.
    - 보상 13~16 은 한계로 다시 자른다.
  - **실효 능력치** 0xb6414 순서로 다시 짰다:
    - 순서: 비율(버림) → 장착 보너스 +30~250 → 무력감·전설·스킬 20. 단계마다 0~999 로 자른다.
    - 원본 비율 표 0xd88aa(60~100%)는 마선수 레벨 배율이다. 육성 선수는 100% 이고, 부상·질병 감소를 이 자리에 둔 것은 추정이다.
  - **장착 아이템** (`equipment.ts`):
    - 부위 헬멧·배트·밴드·슈즈 → 히트·파워·수비·주루. 이름은 StrITEM[부위×11+레벨].
    - 가격은 표 0xcc444(u16) × 1000만, 필요 인기도는 0xcc50a(0/50/150/300/450/600/800).
    - 구매하면 곧바로 장착된다(니블 = 레벨+1). 산 장비는 다시 낄 수 있다 (StrMODE[80]·[81]·[142]).
    - 레벨 7~10 은 히든이다. 오픈 힌트는 StrITEM[202~217].
      부위의 레벨 0~6 을 모두 가지면 레벨 7 이 열린다("컬렉터", 추정). 다른 히든 조건은 아직 없다.
  - **GP 아이템 가격** 표 0xcc41b × 100 G (300×4, 1000, 300, 300, 500, 2000, 500). 사면 바로 쓴다.
    알림은 원문 조각이다(StrMODE[35+k]+[83], [116]/[117], [122~126]).
  - **상점을 원본 세 탭으로 바꿨다** (장착 · 서브 · GP, 0x14a74).
    - 원본에 없던 G포인트 아이템 4종(`shopItems.ts`·`shopItem.ts`)은 지웠다.
    - 선택 처리는 `features/shop/model/shopSelection.ts` 에 있다.
    - 원본의 구매 확인 질문(StrMODE[79])은 생략했다.
  - 관리 화면 "억" 표기: 억 뒤 천의 자리 0 하나만 빼고 그린다(10050 → "1억050"). 억 다음 전진은 폭+3 이다 (0x63118·0x632f6).
- **랄프 11~12회차 (layout-re 2차, 점검 10차 추가분, 누락 탐색 7차)**
  - **관리 화면 하위 메뉴** (0x7e84c):
    - [선수정보]·[트레이닝]·[아이템] 은 화면을 바꾸지 않고 하단 줄을 하위 메뉴(표 0xd4758 칸 5개)로 바꾼다.
      부모 칸은 주황 아이콘으로 남고, (6,245) 칸이면 mode_ui f60 괄호를 덧그린다.
    - 트레이닝 하위 메뉴(히트·파워·수비·주루·필살타법)는 관리 화면 위에 훈련 연출 팝업을 띄우고, 결과를 알림으로 보여 준다.
      따로 있던 훈련 페이지는 지웠다.
    - 아이템 하위 메뉴(장착·서브·GP)는 그 탭의 상점으로 간다.
    - 선수정보 하위 메뉴: 장비착용은 장착 상점, 나머지(기본정보 레이더 차트·아이템/스킬·필살타법·기록실)는
      원본 화면이 아직 없어 성적 화면으로 보낸다 (임시).
  - **등장 애니메이션** (0x7ff8c): 칸이 y170 에서 사인표(0xd310c)로 내려와 조금 넘친 뒤 선다. layout-re 예시값과 같다.
  - **머리띠·바닥띠** (0x54d94, `widgets/screen-frame`):
    - 제목 "나만의리그 타자편", G포인트 알약, 바닥 ⊐ 뒤로 표시.
    - 뒤로 표시가 웹의 나가기·뒤로 버튼을 대신한다.
    - 미끄러짐 시작값 1 은 추정이다.
  - 상점 구매 확인 (StrMODE[79]). GP 탭의 확인 문구는 원문을 못 찾아 같은 꼴로 둔다 (추정).
  - 히든 장비 **컬렉터 조건은 레벨 8** 을 연다 (0xa5020 → 타자 id 36/40/44/48).
    원본 오픈 비트는 전역 저장이고 이벤트 304~307(보상 7)·미션 올 클리어 등으로도 열리지만, 아직 컬렉터만 본다.
  - **휴식** (0x18e3c·0x1b308):
    - 사기 +10~15 를 올린다.
    - 이어서 질병은 60%, 부상은 30% 확률로 낫는다. 못 나으면 남은 기간이 줄고, 기간이 0 이면 반드시 낫는다.
    - 사기 100 이면 아파도 거절한다 (StrMODE[91]).
    - 알림은 "사기 +N 상승하였습니다" 와 회복 문구다.
    - 질병 기간은 3 (표 0xd4d98)이다. 부상 기간 3 은 추정이다.
  - **외출 효과 표** (0x15234): 인기도·평판·사기를 u8 [a, b) 로 뽑는다. 야구교실·CF 는 사기가 내려간다.
    - **CF 는 코드대로 평판이 오른다** (+8~10). 설명서 문구 "평판이 하락한다" 와 반대라 코드를 따랐다.
    - 뽑는 순서(인기도→평판→사기)는 추정이다.
    - 입원 회복은 같은 구조에 질병 90%·부상 70% 다 (0x1575c, 추정).
    - 서브 아이템 k 5~9 보정은 0x152ac 에서 확인됐다 (점검 10차).
- **랄프 13회차 (누락 탐색 7차 TOP 4·5·7, 점검 11차)**
  - **히든 오픈 id** (0x61f5c · 0x62368):
    - 타자 장비 id 는 35 + 부위×4 + (레벨−7) 이다.
    - 원본은 전역 저장이라 기록연감(`compus-baseball/collection`)에 모으고, 앱이 선수 기록으로 옮겨 상점이 판정한다.
    - 여는 경로: 이벤트 보상 종류 7(|값| = id, 이벤트 304~307), 컬렉터(레벨 0~6 모두 → 레벨 8, 구매 직후 기록),
      미션 28개 올 클리어 → id 37 (헬멧 레벨 9, 0xa5184).
    - 오픈 알림 StrCOMMON[139]+[143] "히든 아이템 오픈!! [이름] 나만의리그 타자편에서 사용가능합니다" 를
      장비 구매(컬렉터) 알림과 이벤트 뒤 관리 화면 알림에 붙인다 (랄프 14회차). 원본 팝업 모양은 아직 아니다.
    - 우승 횟수·플레이 시간·대전 등 나머지 조건은 해당 기능이 없어 빠져 있다.
  - **장착 아이템 가격은 표 0xcc444 행 4~7** 이다. 타자는 44칸 뒤를 쓴다 (0x14aaa, 점검 11차).
    그래서 헬멧 5000만~, 밴드 3000만~ 이다.
  - **GP 능력 아이템**: 기본값 +10 뒤, 실효 능력치(장착 보너스 포함)가 한계를 넘으면 기본값을 한계로 둔다 (0xa4528).
    장착 보너스가 있으면 기본값이 한계까지 오르고, 한계를 넘었던 기본값은 내려간다 — 원본 동작 그대로다.
  - **또또 아차상**은 bfa55(0,4) = 능력치 아이템 0~3 이다. 상 알림은 받은 아이템 이름(StrITEM[98+k])을 쓴다.
  - GP 능력 아이템 알림은 "히트 +10 상승하였습니다" 다. 원본 숫자 인자(6·8)의 뜻은 미확인이라 +10 으로 둔다 (추정).
- **랄프 17~18회차 — 투구 AI · 3D 곡선 · 타자 레이어 (position-re 5차, pitch-port 포크)**
  - **CPU 투구**: 보유 구질 목록(0xb6d2c), 구질 선택(0x344dc), 목표 종류(0x9eeac), 목표점(0x345fc), 제구 오차(0x4dc78).
    - 목표 종류는 `pitchpattern_*.arr` 를 생성기가 옮긴 표로 고른다. 난이도는 환경설정에 없어 normal 로 고정했다.
    - 난수 순서: 구질 → 종류 → 목표점 → 제구 등급 → 오차.
  - **투수 레코드**: +0xb 상위 니블 = 폼. 마선수 5명이 폼 6~10 이다. +0x1c 는 구질 비트, +0x18 은 마구 번호다.
  - **3D 곡선·투영** (`entities/pitching/model/pitchCurve.ts`):
    - pitch.zt1 레코드 369개를 옮겼다. 폼 1·3·5·6 은 좌우 반전한다.
    - 발사점 이동, 목표 방향 휘기, 정수 베지어, 투영 0xbe3d8 을 옮겼다. 존 중심 화면 (117,255)·발사점 (73,167) 로 검산했다.
    - 정수 삼각함수 표와 역탄젠트 이진 탐색은 원본대로다 (89°에 닿지 못하는 것까지).
  - **타자 레이어** (0xb905c · 0x78cfc): 자세 프레임, 몸통 앞 레이어(f+14), batter_batter, 겹침 순서 표 0xd3a54 와 규칙 0~3, 잔상.
  - **점검 10차 배팅 지적 반영**:
    - 스킬 31 은 0부터 센 타순 2~4 에서만 적용한다.
    - 스킬 16·17 은 최근 두 타석 기록 코드로 켠다 (코드 매핑은 추정).
    - 스킬 보정은 차례대로 더한다.
    - 방향 가드, 미션 안내 줄 넘침도 고쳤다.
  - **추정**:
    - 마구(구질 22, B-스플라인)와 견제(목표 종류 4 → 1)는 없다.
    - 일반 상대 투수는 원본 명단 첫 선수(봉은중) 구질에 폼 0 이다.
    - 스트라이크 존 반폭은 월드 331/329 로 둔다.
    - **웹 화면은 side 1(좌타) 배치라 타자 side 를 1 로 둔다.** 등록 화면의 좌우 선택은 아직 판정·화면에 이어지지 않는다.
    - 머리·손 아이템 없음, 다리 leg_0, 몸통 balancer 로 그린다.
  - 남은 차이: 사용자 투구(투수편)는 아직 2D 추정 곡선이다. sluger 몸통·에이스 타자 파일은 쓰지 않는다.
- **랄프 18회차 — 선수 등록 · 마선수 등판 (누락 탐색 7·8차)**
  - **선수 등록 화면**: 이름(한글 4·영문 8 = CP949 8바이트, StrMODE[3]), 포지션 내야/외야(StrMODE[4]), 배팅 타입, 좌우(StrMODE[9]), 확인 질문 StrMODE[2].
    - 시작 능력치는 표 0xcc3fa 에 내야는 수비 +30, 외야는 주루 +30 이다.
    - 타입 이름은 원본에 글자가 없어(체형 그림) 몸통 파일 이름 "밸런서/슬러거" 로 둔다 (추정).
    - 타순·피부 선택은 원본 연결을 못 찾아 뺐다.
  - **정규 경기의 무작위 마선수 등판을 없앴다.** 원본에는 그런 코드가 없고, 마선수 대결은 r_event `match` 명령 26건(team 16~20 = 마투수 5명, 승/패 이벤트 분기)으로만 일어난다.
    `match` 대결 화면은 아직 없다 — 다음 과제다.
  - 누락 탐색 8차: 원본은 동료·상대 타석과 상대 공격을 실제 경기 엔진(물리+AI)으로 돌린다.
    웹의 `simulateAtBat` 확률표는 원본 대응물이 없는 추정이다. 기록달성 G·순위·포스트시즌은 여전히 미해독이다(0x44944).
- **랄프 19회차 (layout-re 3차, 누락 탐색 9차 중간, match-port 포크)**
  - **마선수 대결** (r_event `match` 26건):
    - 이벤트가 match 에서 멈추고, team(16~20 = 마투수 5명)에 맞는 타자편 공략 미션으로 대결한다.
    - 승패에 따라 결과 이벤트(114/115 등)로 이어진다. 대결 전 보상·본 이벤트는 결과 이벤트까지 들고 간다.
    - 대결 형식이 공략 레코드(1타석 안타)라는 것은 추정이다. 원본 대결 연출은 없다.
  - **기본정보 카드** (0x15e20 · 레이더 0x5a990):
    - 선수정보 → 기본정보는 상태판 자리에 카드를 그린다 (하위 메뉴 유지).
    - ABILITY 레이더: 중심 (178,104), 거리 floor(값×30/999), 축 이름 상자 slt_frame 17, 숫자, 비교 색.
    - 정보 칸: 팀명·이름·타입·필살·보직·손·피부·타순 (0x7c450).
    - 추정: 축 좌표 반올림, 카드 판 모양, 숫자 색 표현, 필살 이름.
  - **선수 등록 이름표를 원본 문자열로**: 타입 "타격형/장타형" (0x1400258), 피부 "황인/백인/흑인" (0x140022c). 피부 선택을 넣었다(그림 팔레트는 아직 안 바꾼다).
  - **상세정보 결과 창** (0x8a0a4 · 0x872d4): 모델·컴포넌트(`pages/management/lib/detailPopup.ts`, `ui/DetailPopup.tsx`)를 만들었다. 세션 연결은 다음 회차에 한다.
  - **기록달성 G** (0xa77f0 · 0x4ea0c · 금액표 0xd8158, 누락 탐색 9차):
    - 경기 끝 G포인트 = 달성 기록 금액 합이다. 예전의 출전·안타·승리 보너스(추정)는 지웠다.
    - 웹은 사용자 타석 기록(3루타, 솔로~만루 홈런, 연타석 3~5, 한 타자 2~4홈런, 사이클링, 2·3볼넷)과 경기 끝 점수차 승만 센다.
    - 점수차 승은 가장 큰 단계 하나만 준다(추정).
    - 랄프 21회차 정정: 홈런 단계는 그 플레이 득점 수(1~4)로 가린다 (0xa8024). 연타석은 최근 타석이 모두 안타여야 하고, 볼넷도 연속을 끊는다 (0xa76f0).
    - 결과 화면에 달성 기록 이름(StrGAME[id+8])을 보여 준다.
- **랄프 20회차 (점검 12차, 누락 탐색 9차 최종)**
  - **부상 발생을 원본대로** (0x1b4c4):
    - 경기 뒤가 아니라 훈련 결과 창을 닫을 때 한 번 굴린다.
    - 확률 표(사기 >70 0% · 51~70 1/3% · 31~50 3/5% · 11~30 5/8% · ≤10 10/16%, 오른쪽이 필살타법), 유리몸 +5, 행운 −20.
    - bfa55(0,10000) < 확률×100 이면 부상. 기간 3, 알림 "부상을 당했습니다." 이다.
    - 예전 추정 확률(사기 50 미만 최대 35%)은 지웠다.
  - **훈련·휴식 흐름** (0x12e80 · 0x18dd8 · 0x872a1 · 0x1261c):
    - 훈련: [%s훈련]을 하시겠습니까? (StrMODE[85]) → 연출 → 상세정보 결과 창 → 닫으면 부상 판정 → 메인 메뉴로 돌아간다.
    - 휴식: [휴식]을 취하시겠습니까? (StrMODE[90]) → 결과 창("사기 N 상승하였습니다") → 닫으면 회복 판정이다.
    - 질문 창 모양은 추정이다.
  - **하위 메뉴 부모 칸**은 제자리에서 (6,245) 로 같은 사인 곡선으로 미끄러지고, 도착하면 괄호를 그린다 (0x8003c).
  - **상점·장비착용 분리** (0x13506 · 0x17b66):
    - 상점에서 가진 장비는 StrMODE[78] 알림뿐이다.
    - 다시 끼기는 선수정보 → 장비착용(가진 장비만, [81] 확인 → [142], 끼고 있으면 [80])에서 한다.
  - 구매 확인 원문 마크업: 금액 서식 0x55cf4("%d"·"%d억"·"%d억%03d", "만" 없음), GP 는 StrMODE[82].
    휴식 문구는 "사기 12 상승하였습니다" 다.
    뒤로 표시 y = B−16, G 알약 폭 60 이다.
  - 누락 탐색 9차에서 원본 **간이 시뮬레이터**(0xc0d3c~, 동료·상대 타석과 다른 팀 경기), **순위표·9일 라운드로빈 일정·4위 이내 계단식 포스트시즌**, **콜드게임(7회 이후 10점)** 규칙을 찾았다.
    시뮬레이터 수치 명세를 요청했고, 받으면 옮긴다.
- **랄프 21~22회차 — 타석 화면 배경·투수·판정 (position-re 6차, stage-port 포크)**
  - 하늘: 색 번호 표 0xd37a4 · 색 쌍 0xd37f2 (생성기 `stadiumScene.ts`), 열 = min(이닝,12).
  - 구름: 3틱마다 i+1 px 이동, 480 되감기, 밤(색 9)에는 안 그린다.
  - 펜스·관중석·팀 아이콘·전광판: PZD/PZF 짝 디코딩(`decode_split_pair`), 좌타 기준 (−131,−67), 원본 그리는 순서.
  - 투수: 폼별 단계 표 0x9e0b8, 유지 dur+1, 대기 [0,19,0,19,0]. 공은 단계 6 뒤부터 난다.
  - 판정 글자: game_judge 애니, (117,224).
  - position-re 합성 그림(orig6_side1)과 겹쳐 본 결과 하늘·구름·펜스·관중석·그라운드·투수·존이 일치한다.
  - 추정: 하늘 행 rand(0,6), 구장 0, 전광판 첫 프레임 고정, 판정 지연 1 = 1틱, 마선수 투수 단계 표 미이식.
  - 남은 것: 우타(side0) 배치, 구장 고르기 규칙, 직구 외 릴리스 좌표, 수비 화면 전환.
  - 포수·심판은 원본 타석 화면에 없다 (0x4d2ac 호출 목록 확인) — 웹도 그리지 않는다.
- **랄프 22회차 — 경기 종료 규칙** (0xb68fc, 누락 탐색 9차):
  - 9회 이후 말 공격 중 앞서는 순간 끝난다 (끝내기).
  - 9회 이후 초가 끝났을 때 우리(홈)가 앞서면 말 없이 끝난다.
  - 말 3아웃에 동점이면 연장이다. 상한 12회는 추정이다(원본은 상한 코드가 없다).
  - 7회 이후 10점 차면 콜드게임이다 (초 끝 우리 10점 앞섬 / 말 끝 상대 10점 앞섬).
- **랄프 23회차 — 리그 도메인** (`entities/league/model/league.ts`, 누락 탐색 9차):
  - 일정: 표 0xd89cb 9일 라운드로빈 (바이트 확인).
  - 승패: 0xb76dc·0xb77e0 (승·패·연승·최다 연승·연패·상대 전적).
  - 순위: 0xb79d8 승 → 패 → 상대 전적. 그래도 같으면 팀 번호 순이다(추정).
  - 포스트시즌: 4위 이내, 준PO 3v4(5전3선승) → PO 2위(5전3선승) → KS 1위(7전4선승).
  - 아직 경기 흐름·시즌 종료에 연결하지 않았다. 다른 팀 경기 결과를 원본 간이 시뮬레이터로 만든 뒤 붙인다 (명세 대기).
- **랄프 30회차 — 원본 메시지 상자** (0xbbef8 → 0x74ef4 · 0x746cc, layout-re 4차):
  - 관리 화면 알림과 예/아니오 질문을 원본 상자로 바꿨다 (`pages/management/ui/MessageBox.tsx`).
    - 화면 전체를 검정 반투명으로 덮는다. 폭 240 띠를 세로 가운데에 둔다.
    - 판 #335FCD 에 위·아래 5줄 테두리(#1E1AA6·#FFFFFF·#7B9EFF)를 긋는다.
    - 글은 (45, y+20) 부터 폭 150, 줄 간격 14 다. 버튼은 글자이고 가로 간격 40 이다.
  - 추정: 버튼 글자("확인"·"예/아니오" — 원본 문자열표 미확인), 선택 색(노랑), 열림 애니메이션 없음.
  - layout-re 4차의 **기록실 순위표**(0x7f070: 10줄, img_text 65+팀, 승률 = 승×100÷(승+패) 정수 % + num 106,
    사용자 팀 강조 없음), **필살타법 칸**(0x805a8), **포스트시즌 대진표**(0x853ac)는
    리그 데이터가 시즌에 연결된 뒤 붙인다 (명세: scratchpad/layout/screens_spec_4.json).
- **대사·에피소드 데이터를 JSON 으로 분리** (사용자 요청):
  - `src/shared/config/original/data/*.json` 에 원본 데이터를 두고, 같은 폴더 TS 모듈은 타입만 입혀 읽는다.
    events(에피소드 스크립트, 728K)·userEvents·howto·tips·titles·endings·items·skills·roster 가 JSON 이다.
  - 생성기 `tools/generate_game_data.py` 의 `write_json_module` 이 JSON 과 로더를 함께 만든다.
  - `src/shared/config/original/index.ts` 배럴로 한 번에 꺼내 쓴다. 데이터 설명은 `data/README.md` 에 있다.
- **에이전트 4개 체계로 전환** (사용자 요청): bug-fixer(버그) · design-fixer(화면) · qa-checker(원본 대조 QA) · planner(할 일 목록).
  planner 의 1순위는 원본 간이 시뮬레이터(0xc262c 계열) 이식 명세다.
- **에이전트 4개 체계 1회차 (bug-fixer · design-fixer · qa-checker · planner)**
  - **빌드 도구를 Yarn Berry(v4) Zero-Install 로 바꿨다.** PnP(.pnp.cjs)라 node_modules 가 없다.
    `yarn verify` 로 돌린다. 설치 재현은 0.3초다. npm 은 쓰지 않는다 (package-lock.json 삭제).
    Vite 가 PnP 경고를 내지만 빌드·테스트는 정상이다.
  - **훈련 스킬 보정 누락 수정** (QA 대조): 능력 훈련에 병아리(0) 상승 +1·사기 −1, 몹쓸몸(3) 상승 −2·사기 +2 를 넣었다 (StrMODE[201]·[202], 0x1891c·0x1898e).
  - **필살타법 G포인트 차단 제거**: 원본은 G 가 모자라도 훈련을 막지 않고 0 에서 바닥만 친다 (0xa3c84).
    최고 레벨 차단은 원본 가드를 못 찾아 남겨 두고 추정 표기했다.
  - **기록달성 주석 정정**: 지급 표는 0xcfbf8, 경기 중 누계 표가 0xd8158 이다. 점수차 승 "하나만" 은 0xa7f22 로 확정됐다.
    원본은 기록을 **팀 단위**로 세므로 동료 타석·수비/투수 기록 23종이 웹에 빠져 있다 — 간이 시뮬레이터와 함께 채운다.
  - **화면**: 기록실 팀 순위표(`widgets/standings`)·필살타법 창(`widgets/special-swing`)을 명세대로 새로 만들었고,
    메시지 상자 글자 규격(11px·줄 간격 14)을 원본에 맞췄다. 명세 합성 그림과 픽셀 대조로 확인했다.
  - **버그 수정 2건 (bug-fixer, 둘 다 실패 테스트 먼저)**
    - 관리 화면 알림이 두 번째부터 안 뜨고, 다른 화면을 다녀오면 지난 알림이 되살아났다.
      알림 소유를 세션으로 올리고 [확인] 이 실제로 알림을 지우게 했다.
    - 메시지 상자가 떠 있어도 뒤쪽 화면이 같은 키를 받아, 키보드로 구매를 끝낼 수 없고 Escape 가 관리 화면을 빠져나갔다.
      상자가 잡는 단계에서 키를 가져가고 전파를 막는다 (←→ 이동 · Enter 선택 · Escape 는 마지막 버튼).
    - 훈련 칸 4(필살타법)에는 StrMODE[85] 질문을 띄우지 않는다 (원본은 칸 0~3 에만 붙는다).
    - 검증만 하고 버그 없던 것: 마선수 대결 승/패 경로(훅 테스트로 구분 확인), 세이브 형식 1→2 환산과 왕복, 외출 흐름.
  - **리그 전적 연결**: `PlayerCareer.league` 를 두고 경기가 끝날 때마다 `recordLeagueResult` 로 쌓아
    기록실 순위표(0x7f070)에 그대로 넣는다 (0xb76dc 승·0xb77e0 패, 무승부는 어느 쪽도 세지 않는다).
    한때 넣었던 "같은 날 나머지 4경기를 팀 전력 비로 가르는" 임시 규칙(`playLeagueDay`)은 **원본 근거가 없어 걷어냈다** —
    지금 순위표에는 **내 팀 경기만** 쌓이고 나머지 팀은 0승 0패로 보인다.
    원본 간이 시뮬레이터(0xc2dac·0xc2a48)를 옮기면 다른 팀 경기가 채워진다.
- **랄프 루프 종료 (30회차)**: 마지막 상태는 `npm run verify` 통과(테스트 693개, 빌드 성공)다. 커밋은 하지 않았다.
  - 서브에이전트 세 곳(gap-finder·position-re·checker)이 사용량 한도로 멈춰, 아래 보고는 받지 못했다:
    간이 시뮬레이터 이식 명세, 타석 화면 7차(구장·수비 화면·우타 배치), 점검 13~15차 결과.
  - 다시 시작할 때는 그 세 보고부터 받고, 아래 남은 일 목록 순서대로 이어 가면 된다.
  - 점검 14차가 브라우저에서 확인하던 것: 마선수 대결 **승리 경로**(이벤트 112 → 대결 승 → 114 보상)는 아직 확인되지 않았다.
- **타격 판정 0xab214 전문 해독 → 이식본 정정 (2026-09-18)**: 디컴파일 전문과 한 줄씩 대조해 네 곳을 고쳤다.
  수치는 함수 리터럴이 아니라 `base/work/jar/data/d_level.dat` 에 있고, 읽기 실패용 폴백(0xb6f28)과 값이 완전히 같다.
  - **+500 의 정체**: 예전 이식본이 "직전과 같은 구질이면 B·C +500" 으로 읽은 값은 사실 `param_15 × 500` 이고
    두 호출자 모두 1 을 넘긴다 — 즉 **조건 없이 늘 붙는다**. `isSamePitch` 와 그 배선(`previousPitch`)을 걷어냈다.
  - **마선수 보너스**: "내 선수" 가 아니라 **마선수(isAce = 선수 레코드 [10] 부호 비트, 0xb6388)** 것이다.
    육성 모드에서만 켜지고, 빠져 있던 **투수 쪽 보너스(400−40×팀레벨)** 와 미션 모드 +100 을 새로 넣었다.
  - **마선수 계수**(250·400·430·K 1000)는 타자·투수 중 한쪽이라도 마선수일 때만 쓴다. 전에는 나만의리그면 늘 썼다.
  - **탈진 보너스 2000** 은 B 는 배율 **뒤**, C 는 배율 **앞**에 붙는다 (원본 순서).
  - 타석 화면은 마선수가 등판하면 `isPitcherAce` 를 넘긴다. 팀 레벨은 웹판에 아직 없어 0(보너스 최대)으로 둔다 (추정).
- **간이 타석 엔진 이식 (0xc11f0) — 발명한 확률표 제거 (2026-09-18)**
  원본은 사람이 조작하지 않는 타석도 **사람 타석과 똑같은 0xab214** 로 판정한다. 동료 여덟 타순, 상대 공격,
  하루치 다른 팀 경기(0xc2a48)가 전부 이 길을 쓰며, **점수를 뽑는 공식 같은 것은 없다**.
  - `entities/game/model/quickAtBat.ts` — 코스 오차 rand(−13,21), 스윙 세기 75, 30%(0xc1584)·15%(0xc1590) 보정,
    연장 보정, 구위 등급표 0xd896c(0xb74bc), 결과 코드 분기(땅볼 주력 판정·파울·한 루 더 가기 0xc1804).
    **원본 간이 타석에는 볼 카운트가 없다** — 스트라이크 셋이면 삼진이다.
  - `entities/game/model/simulateHalfInning.ts` — 3아웃까지 타석을 돌려 점수를 읽는다.
  - `entities/team/model/teamRoster.ts` — 원본 Team 구조체대로 팀당 타자 12·투수 8 로 명단을 자른다
    (타자 180 = 15×12, 투수 120 = 15×8 로 정확히 나뉘는 것을 테스트로 확인).
  - **삭제**: `simulateAtBat.ts` 의 `TEAMMATE_OUTCOME_WEIGHTS`·`OPPONENT_INNING_RUN_WEIGHTS` — 원본 대응물이 없던 표다.
  - 남은 추정: 부호 난수(디컴파일에서 인자 유실), 상대 타순이 이닝마다 1번부터 시작, 투수 교체 없음(선발 고정),
    뜬공/직선타 구분(원본은 코드 0 하나).
- **하루치 리그 경기 이식 (0xc2a48) — 순위표가 실제로 찬다 (2026-09-18)**
  `entities/league/model/leagueDay.ts` 가 일정표 0xd89cb 로 그날 다섯 대진을 짜고, 내 팀 경기만 빼고
  나머지 네 경기를 9이닝(동점이면 연장, 상한 14회 추정)까지 같은 타석 엔진으로 돌린 뒤 승패를 기록한다.
  경기 정산에서 `applyLeagueDay` 로 불린다 — 원본도 0x4ea0c 안에서 0xc2a48 을 따로 부른다.
  **무승부가 없다**: 리그 구조체에 무승부 칸이 없어 점수가 같으면 한쪽이 승이 된다 (여기서는 홈 팀, 추정).
  기록실 순위표에 이제 열 팀 전적이 모두 쌓인다 — 전에는 내 팀만 보였다.
  - **감각 확인 (100경기·5000타석 돌린 실측)**: 경기당 평균 득점 원정 5.08 · 홈 4.71 (최다 24),
    타석 분포는 뜬공아웃 26.1% · 땅볼아웃 26.0% · 1루타 20.6% · 삼진 14.4% · 2루타 9.9% · 홈런 2.1% · 3루타 0.9%.
    야구다운 분포가 **확률표 없이 원본 판정식만으로** 나온다는 것이 이식이 맞다는 근거다.
- **동료 타석 기록달성 연결 (2026-09-19)**: 원본은 기록을 **팀 단위로** 센다 — 0xa77f0 게이트는 "공격 팀이 사람 팀인가"만 보고,
  0xa8024 의 "본인인가" 필터는 **개인 통산 성적 증가만** 감싼다. 그래서 동료 여덟 타순의 3루타·홈런 단계·연타석·사이클링·볼넷도
  G포인트로 들어와야 하는데 웹판은 사용자 타석만 세고 있었다 (qa-checker 1회차 지적).
  - `entities/game/model/batterGameLog.ts` 를 만들어 **사용자와 동료가 같은 `recordBatterAtBat` 을 쓴다.**
    `gameFlow.ts` 안에 있던 `isCycleOf` 와 기록 판정 중복을 이 모듈로 모았다.
  - `GameProgress.teammateLogs` 가 타순별로 따로 기록을 들고 있다 — 한 사람 것으로 뭉치지 않는다.
  - 이게 가능해진 이유는 동료 타석이 확률표가 아니라 원본 엔진을 쓰게 됐기 때문이다(위 항목). 순서가 맞아떨어졌다.
- **QA 1회차 지적 수정 3건 (2026-09-19) — 전부 바이너리를 직접 디스어셈해 확인했다**
  (스크래치패드가 날아가 있어 capstone 으로 다시 읽었다. 방법: 파일 오프셋 = VA − 0xfcc, Thumb 모드.)
  - **훈련 횟수가 아무 데서도 기록되지 않았다** — `trainingCounts` 를 읽기만 하고(`titles.ts`) 쓰는 코드가 없어
    칭호 23(200회)·24(100회)를 영원히 못 땄다. `countTraining` 을 만들어 `runTraining` 이 능력 훈련·필살타법
    양쪽 모두 세게 했다. 죽은 코드였던 `trainingCountOf` 가 이제 쓰인다.
    테스트는 필드를 직접 만들어 넣지 않고 **실제로 100번 훈련해서** 합계를 확인한다.
  - **콜드게임 판정 시점** (0xb6976 직접 확인): 원본은 말 공격 중 **아웃 수와 무관하게 매 타석** 10점 차를 본다.
    웹판은 초 끝과 말 3아웃만 봐서 7회말 무사에 10점 차가 나도 8회초까지 경기가 이어졌다.
    디스어셈 요지: 이닝 > 5 이면 `state[9]==1`(말)일 때 아웃 ≤ 2 라도 0xb6976 으로 떨어져 `score0+10 <= score1` 을 본다.
  - **`STARTING_GAME_POINT = 300` 은 근거 없는 값이었다** — 신인 초기화(0x11244~0x112d2)가 쓰는 값은
    연봉 50(0x32)·소지금 60(0x3c)·인기도 0·평판 300(0x96<<1)·사기 100(0x64) 뿐이고 **G포인트를 넣는 곳이 없다.**
    평판 값을 베낀 것으로 보여 **0 으로 바로잡았다.** 기존 테스트 775개가 그대로 통과해 의존하던 곳도 없었다.
## 배경·메뉴 위치 조사 (2026-09-19, 사용자 지적 "뒤 배경이 검정으로 나온다")

브라우저로 타이틀 → 메인 메뉴 → 선수 등록 → 이벤트까지 돌려 보고, 스프라이트 142폴더를 전부 훑었다.

**원본에는 전체 화면 배경 그림이 딱 하나뿐이다.** 200×200 이상인 그림은 셋뿐이고,
그중 240×320 은 `main_title/000`(타이틀) 하나다. 나머지는 `event_map/000`(240×297 외출 지도)와
`defense/000`(310×500 수비 화면)이다. **즉 원본은 메뉴·관리 화면 배경을 그림이 아니라 채우기+스프라이트로 그린다.**

검정으로 보이는 원인은 두 가지다:
1. `RawScreen` 이 240×320 칸 **바깥**을 `ORIGINAL_COLORS.black` 으로 칠한다 — 레터박스라 이건 맞다.
2. **화면 15개가 아직 웹 전용 `PixelScreen` 껍데기**(남색 `#05070d`, 원본에 없는 색)를 쓴다.
   `create-player` · `story` · `game` · `pitching` · `mission-play` · `mission-select` · `outing-map` ·
   `shop` · `record` · `help` · `settings` · `special` · `ending` · `game-result` · `season-end`.
   이 화면들은 칸을 채우지 않아 빈 곳이 통째로 어두운 남색이 된다.
   특히 선수 등록의 "이대로 결정 하시겠습니까?" 확인 화면은 거의 전부가 빈 칸이다.

**이벤트 화면은 고쳤다** — 원본은 이벤트를 별도 화면으로 띄우지 않는다.
`events.json` 의 `trigger` 가 **이벤트가 어느 화면 위에 뜨는지**를 가리킨다 (0 관리 화면 · 1 외출 지도 · 2~6 장소).
웹판은 trigger 0 이벤트를 검은 `PixelScreen` 으로 따로 띄우고 있었다.
`CareerRoutes` 가 관리 화면을 깔고 그 위에 `StoryScreen` 을 덮개로 얹도록 바꿨다.

**아직 못 고친 것**: 나머지 14개 화면. 원본 함수를 모르는 것(`create-player`·`help`·`settings`·`special`·
`ending`·`game-result`·`record`)은 지어내지 않고 남겨 뒀다. 메인 메뉴의 셀렉트박스는 2026-09-13 사용자 요청으로
바꾼 것이라 그대로 뒀고, 배너(`mode_back/000`, 240×84) 아래가 비는 것은 원본 목록 배치(`main_ui` 002·003)로
바꿀 때 함께 풀린다.

- **랄프 27회차 — 남은 일 목록 (우선순위 순, 서브에이전트 결과 대기 포함)**
  1. **간이 시뮬레이터 이식** — 타석 엔진(0xc11f0)·하루치 리그 경기(0xc2a48)·정규시즌 종료(0xb818c)까지 **끝났다**.
     `entities/league/model/seasonEnd.ts` 가 45경기째에 순위를 매겨 1위 횟수(레코드 +0x7a)를 세고
     준PO 대진(0xb80a8)을 연다. `PlayerCareer.postseason` · `regularSeasonFirstCount` 에 담긴다.
     남은 것: **포스트시즌을 실제로 치르는 것** — 내 팀 차례면 사람이 경기하고, 아니면 CPU 끼리 돌린다(0xc2760·0x13da0).
     화면(대진표 0x853ac)은 design-fixer 가 prop 만 받는 형태로 만들던 중 한도로 멈췄다.
     시즌 끝에 4위 이내면 포스트시즌(CPU 시리즈 0xc2760, 사용자 경기, KS 우승 StrMODE[137])을 진행하고,
     정규시즌 1위 횟수(+0x7a)를 센다.
  2. ~~**기록실 = 팀 순위표 화면** (0x7f070)~~ — 완료. 창·머리칸·줄 배치와 하늘색 순위 숫자까지 명세대로 만들고
     `career.league` 를 붙였다. 남은 것은 다른 팀 경기(1번 항목) 뿐이다.
  3. **알림·질문 창 원본 모양** (layout-re 4차): 지금은 추정 상자다.
  4. **점검 13~15차 결과 반영** (투구 AI, 마선수 대결, 기록달성, 기본정보, 훈련·휴식 흐름, 타석 배경, 경기 종료 규칙).
  5. **타석 화면 나머지** (position-re 7차):
     - 우타(side0) 배치 — 등록 화면의 좌우 선택을 판정·화면에 잇는다
     - 구장·하늘 행 고르기, 전광판 애니, 마선수 투수 단계 표 0xd7483, 직구 외 릴리스 좌표
     - 수비 화면 전환
  6. **필살타법 창·아이템/스킬 창** (0x803d4·0x81dc0), 필살 스윙(키 0) 사용.
  7. **투수편 전체**: 사용자 투구 3D 곡선, 투수 훈련·구질 훈련, 마구.
  8. **피부색 팔레트** (0x10810 → 0xcaa00 미해독). 선수 등록 타순 선택 연결(미해독).
  9. **시즌모드·홈런더비·대전모드·일반모드** — 메뉴만 있고 기능이 없다.
  10. **MVP 판정** — 미해독.
- 미해독(L): (기록달성 G 는 0xa77f0 에서 해독됨 — 0x44944 는 경기 화면 그리기였다), 포스트시즌·순위·CPU 시뮬레이션, MVP 플래그, 평판 변화 뒷부분, 타격 판정(0xab214), 공 궤적 이식

**원본에 있으나 아직 없는 것** (감사 결과, 우선순위 순):
- 일반모드·시즌모드·홈런더비·대전모드, 스페셜, 환경설정(속도 옵션)
- 나만의리그 투수편 전체, 포스트시즌·7~13년차·엔딩 22개·연봉·명예의전당, 경기 후 감독 평가(StrUSER_EVT)
- 스킬 80, 목표설정, 돌발미션(`Xls*_BURST` 는 필살기가 아니라 돌발미션 조건·대사), 부상·질병 발생
- 원본 아이템 255 체계(장착·서브·GP, 소지금 결제) — 지금 상점 4개는 원본에 없는 것
- 수비·송구·주루 조작, 필살타법·마구와 연출, 콤보·결과 그림. 동료 타석·상대 공격은 원본 로스터 대신 확률표
- 쓰지 않는 스프라이트 97폴더(수비수 11·장비 외형 57·필살기/구장 연출 등), `s_event` 30개(시즌 스토리)
- 원본 근거 없는 수치: 트레이닝 비용·상승치, 시작 능력치, 능력 상한 100(원본 999), PERFECT 보정, 도루·마선수 등판 확률

## 남은 문제

## r_event.zt1 — 미해독 (세 가지 독립 검증으로 확인)

33,836바이트. 헤더 `ca 00 c3 01`. 다른 모든 포맷과 달리 **개수+오프셋 구조가 아니다**.

알아낸 것:
- 가장 흔한 명령 길이는 **14바이트** (`00 02 01 01` 패턴 간격이 14로 733회)
- 14바이트로 정렬되는 구간에서는 4번째 바이트가 순차 증가한다 (05,06,07,09,0a,0b,0c,0d)
- 그러나 파일 전체로는 열별 값 분포가 균일하다 → **가변 길이 명령**이다

시도하고 실패한 것 (전부 재현 가능):
1. 개수+오프셋 가정 — u8/u16 개수 × u16/u32 오프셋 네 조합 모두 테이블 끝과 첫 오프셋 불일치
2. 고정 레코드 가정 — 14바이트 정렬 시 열별 분포가 구분되지 않음 (서로 다른 값 138~172개로 균일)
3. 텍스트 인덱스 탐색 — 간격 2~40, 시작 0~39, 8/16비트의 모든 조합에서
   1770(텍스트 줄 수)까지 가는 단조 증가 수열이 없음. 최장 53개, 최대 255

**결론: 스크립트 VM의 바이트코드다.** `binary.mod`에서 인터프리터 루프를 찾아
opcode 표를 먼저 해독해야 하며, 이는 Ghidra급 디컴파일러가 필요하다.
이 때문에 **스토리 장면 경계 · 이벤트 발동 조건 · 지도 건물 좌표**가 추정으로 남는다.
- 웹에서 실제 스크린샷을 찾지 못했다. 해외판 이름은 **Baseball Superstars 2011**이고,
  리뷰에서 "Every 2 matches, you can either go for training, rest or an outing",
  "places with an exclamation mark", 모드 6개(Exhibition/My League/Season/Homerun Race/
  Mission/Match Play)까지는 확인됐지만 **화면 레이아웃 묘사는 어디에도 없었다**.
  archive.org에 이 게임의 iOS IPA(12.6MB)가 보관되어 있다 — 내려받으면 실제 화면
  에셋을 볼 수 있겠지만, 상용 앱 패키지라 이쪽에서 받지 않았다.
- `main_title.pzx`의 프레임 데이터(32개)는 파서가 잘못 읽는다 (배경 이미지가 이상한
  위치에 반복 등장, 빈 프레임 섞임). 타이틀 배치는 구멍 기법 쪽이 정확하다.
  화면 근거는 전부 패키지 안의 에셋과 게임 내 설명서에서 나온 것.
- 목표설정(연차 5가지), 돌발미션, 스킬 장착, 서브아이템 미구현.
